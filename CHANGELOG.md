# CHANGELOG

## Unreleased

+ [优化] 本地 VST 宿主实现插件运行隔离：每个插件实例独立运行在子进程（`--plugin-worker`），单个插件崩溃或挂起不影响宿主及其他实例，故障实例返回明确错误码，宿主停止时彻底回收所有子进程。
+ [调整] 本地 VST 宿主默认不再要求 token：未设置 `VST_HOST_TOKEN` 时只按 Origin 白名单校验（仍只监听 `127.0.0.1`），宿主一启动浏览器即可直接连上、无需任何 token 配置；需要校验时设置 `VST_HOST_TOKEN` 即恢复 Bearer 校验。
+ [新增] 支持在音轨上插入 VST3 效果器：本地宿主 `load` 新增 `role`（默认 `instrument`，`effect` 按 `Fx` 子类挑选、找不到时报 `no_effect_class`），效果实例启用输入总线并新增 `POST /audio-in`（与 `/audio` 同帧格式、同样校验 Origin 与 token，可与输出流并发）；浏览器侧新增帧写入 helper，worker 把效果轨的音频批量上传、worklet 仅在效果实例时用 `numberOfInputs: 1` 采集输入，效果以内联方式接在 `strip.input` 与静音门之间（推子 / 声像 / 静音独奏 / 发送 / 总线仍在其后生效），轨道对象上用 `vst3Effect` 挂载；本期只做链路与轨道槽位、没有效果器选择 UI，离线导出暂不包含效果处理，宿主侧限制：单次上传 ≤ 1 MiB 与 64 帧、未播放输入最多缓存 48 块（约 256 ms）、worker 最多排队 32 块；本机没有真实 Fx 插件，效果链路只用 VST3 SDK 的 `again` 样例在临时目录验证过。

+ [新增] VST3 乐器插件支持状态持久化：在钢琴卷帘里选择插件时同时写入一个小的 `stateKey`，插件自己的状态（预置等）以 localforage blob 存在 `vst-state:<stateKey>` 下并随项目删除一起回收，重载后自动恢复；打开插件界面期间每 10 秒保存一次、关闭界面（或离开卷帘）时最终保存；已知限制：直接用插件窗口自己的关闭按钮关窗后立刻重载，最后一次改动可能来不及保存。
+ [新增] 新增本地 VST3 音源支持：新增 C++ 伴生进程 `vst-host`（只监听 `127.0.0.1:3211`，复用与 MCP 相同的「精确 Origin 白名单 + Bearer token」信任模型；插件扫描在隔离子进程中执行并带超时；提供 `--scan` / `--selftest` / `--stop`、PID 文件，端口被占用时直接报出占用者 PID 并拒绝启动）。乐器轨的音源除了内置预设，还可以选本机已安装的 VST3 乐器（列表来自宿主扫描，按「名称 · 厂商」展示），音频经 worker + AudioWorklet 接入既有音频图，所以推子 / 声像 / 静音独奏 / 发送 / 总线 / 自动化照常生效；插件界面以宿主进程持有的原生窗口打开（HALion Sonic 载入预置依赖它）。构建与运行方式见 `vst-host/README.md`（需先克隆 VST3 SDK）。
+ [调整] 导出（离线混音 / 分轨）现在会包含 VST3 音源轨：离线导出前由本地宿主新增的 `POST /render` 把每条 VST3 音轨在插件的离线处理模式下（支持时更快于实时）渲染成 stem，帧格式与 `/audio` 相同，采样率与 `Tone.Offline` 一致，并接插件上报的延迟做对齐；stem 作为播放器从 0 时刻经该轨的通道条进入离线渲染，推子 / 声像 / 静音独奏 / 发送照常生效，非 VST3 输出不变。宿主未运行或某轨渲染失败时该轨仍按原行为跳过，混音返回值分别列出已 bounce 与已跳过的轨道，导出界面会弹提示点名被跳过的 VST3 轨。宿主侧新限制：单次请求 ≤ 1800 秒、≤ 65536 个音符，首块等待 15 秒、单块停顿上限 30 秒、未读帧队列上限 64 块（约 341ms）。

+ [新增] 钢琴卷帘工具栏在「音色」选择器旁新增「打开插件界面」：桥接就绪（`graph.vstSources` 里该轨道已有 `instanceId`）时点击即调用本地宿主的 `editorOpen` 打开插件自己的原生窗口（HALion Sonic 载入预置依赖它），未就绪时显示中文状态提示、打开失败显示非致命提示，宿主未运行时该按钮不出现。
+ [调整] 整体风格回到圆角矩形的温和版（撤销上一轮的全站去圆角）：卡片 / 面板 / 分区 / 弹层 / 菜单 / 列表行 / 页栏与侧栏统一为 `rounded-xl`（12px），画布节点卡片保持 `rounded-3xl`（≈22px），按钮 / 输入框 / 下拉 / 标签 / 徽标统一为 `rounded-md`（6px），圆形元素（状态点 / 头像 / 加载圈 / 录音键）不变；Ant Design 圆角 token 回到 8 / 6 / 12 / 4，全局 `--radius` 回到 `0.625rem`。**表格与表格内容按要求保持方角** —— 全仓仅剩的 3 处 `rounded-none` 就是两个库页表格与 providers 表格上的 `!rounded-none` 覆盖，原样保留。共 79 个文件、70 处容器圆角 + 216 处控件圆角。
+ [修复] 修复上一轮「去圆角」脚本误伤的一处注释：audio studio 里 "tab had been backgrounded" 中的单词结尾 `rounded` 被当成 Tailwind 类替换成了 `backgrounded-[2px]`（`backgrounded` 正好以 `rounded` 结尾），已还原，并复查确认全仓仅此一处。

+ [修复] 修复顶部导航与移动端抽屉里「我的资产」入口又出现、且显示成原始 key `navigation.assets` 的问题：`constant/navigation-tools.ts` 里的 `assets` 条目在素材库移除后被某次改动写了回来，而对应的 `navigation.assets` 文案已随功能删除，于是渲染出原始 key；现已移除该条目与不再使用的 `Images` 图标导入，导航恢复为「我的画布 / 文本创作器 / 配置」三项（桌面顶栏与移动端抽屉共用同一份列表）。

+ [新增] 接入 Matilda（Maincode）：新增本地同源代理 `web/matilda-proxy.ts`（Vite dev + preview 中间件），在 Node 侧通过官方 `@maincode-ai/matilda-agent-sdk` 调用 `matilda.maincode.com`，并对外暴露 OpenAI 形状的 `/matilda/v1/responses`、`/matilda/v1/chat/completions` 与 `/matilda/v1/models`，因此浏览器不再被它缺失的 CORS 头拦住；前端只需在设置里粘贴 `mc_live_` 开头的 API key，无需关心模型 id 与能力分类。代理只从请求头透传密钥、不落盘、不写日志，并支持客户端中断时一并中止上游流。注意：SDK 是 Node-only（无 browser 导出、顶层引入 node 内置模块），因此永远不进前端包；生产静态部署需要等价的 Node 函数。
+ [调整] 提供商与模型配置改为「三档预设 + 写死模型清单」，并移除「抓取模型」功能：只保留 OpenRouter（多模态：图片 `openai/gpt-image-2.5-sunburst`、视频 `minimax/hailuo-3-max` / `minimax/hailuo-3`、语音 `fish-audio/s2.1-pro`、音乐 `google/lyria-3-pro-preview`、文本用实时目录逐一核验过的 5 个 id）、DeepSeek（纯文本：`deepseek-v4-pro` / `deepseek-v4-flash`）与 Matilda（纯文本，一个不透明的 `matilda` 条目）。DeepSeek / Matilda 标记为纯文本提供商，其模型一律归类为 `text` 且能力不可编辑，因而不会出现在图片 / 视频 / 音频 / 语音的下拉里；已存渠道的模型列表按预设重置，非预设渠道不会被删除。删除「抓取模型」的原因：实时 OpenRouter 目录（458 个模型）里根本没有本应用使用的图片 / 视频 / 语音 id，抓回来的列表反而不可用。

+ [新增] 图片复制到剪贴板：新增共享 `copyImageToClipboard`（接受 `blob:` / `data:` / `http(s):` 图片，非 PNG 自动转成 PNG，失败返回 false 不抛异常）；图片节点双击预览弹窗、智能画布合成预览弹窗、图片节点悬停工具栏、多图节点展开面板里的每张图卡片都补上「下载 + 复制」一对按钮，并按需求全部改为**纯图标**（保留 `aria-label` / `title` 可访问性），复制成功 / 失败各有提示。同时清掉图片快捷工具里已失效的 `saveAsset`（素材库移除后遗留的死 id），并把快捷工具配置存储键由 `canvas-image-quick-tools-v14` 升到 `v15`，让既有配置也能拿到新的 `copy` 工具；图片节点的悬停工具栏补上原先缺失的下载项（此前只有视频 / 音频有），下载与复制配对完整。
+ [修复] 修复画布节点被误改成方角的问题：节点外壳恢复为圆角矩形（`rounded-3xl`），节点内的内容容器、批量叠层与缩放手柄本就继承外壳圆角因而一并恢复；面板、弹层、对话框、列表与库页表格仍保持方角的表格风格。

+ [调整] 全站去圆角卡片，改为表格 / 分区风格（磨砂玻璃保留）：卡片、面板、分区、弹层、菜单、列表容器与列表行一律改成方角；按钮 / 输入框 / 下拉 / 标签统一为 2px 圆角，只有状态点、头像、加载圈、录音键这类真正圆形的元素保持圆形。列表行改为通栏平铺 + 1px 行分隔线，选中态从"描边胶囊"改成"浅底 + 左侧 2px 品牌色条"；卡片保留磨砂玻璃与 1px 边框但去掉圆角与大投影（仅浮动弹层保留柔和投影）。画布库左栏分组行、写作库（含原先包住表格的卡片外壳）统一成与画布库一致的通栏表格形态；画布节点卡片同步去圆角，尺寸、边框宽度与拖拽 / 命中判定不变。全局圆角 token 归零，Ant Design 圆角改为 2px（容器类为 0）。
+ [修复] 修复 Python MCP 侧遗留的已删除功能定义：`canvas_expand_story` 工具、`expand_story` 操作类型、NodeType 枚举里的 `"story"`，以及三处仍提到故事节点的工具描述全部移除（此前只清理了前端，导致该工具仍出现在工具列表里、一旦调用必然失败）；`site_navigate` 说明补上新版「文本创作器」路径 `/write` 与 `/write/:id`；工具数量声明统一为实际的 30 个。画布「资源」节点的 `assets` 类型及其文案属正常功能，保留。
+ [删除] 移除图像工作台「还没有图层，先添加图片或文字」的空状态提示：画板中央的图层图标浮层与图层面板里的空状态文案一并去掉，同行删掉已无引用的 i18n 键 `canvas.ps.emptyLayers`；空画板与空图层列表不再显示任何提示文字。
+ [调整] 三个工作台统一为全屏使用：进入图像工作台、音频合成器与文本创作器（`/write/:id`）时隐藏顶部导航、只保留一个返回按钮直接回到有顶栏的库页（图像 / 音频回到 `/canvas`，写作回到 `/write`），画布与两个画布工作台之间仍用工作台自带顶栏的工作区页签切换；`/canvas` 与 `/write` 两个库页照常显示顶部导航；返回按钮文案同步改为「返回画布库 / Back to canvas library」与「返回作品库 / Back to library」，不再写成"返回画布"。

+ [调整] 全站恢复磨砂玻璃质感并把所有卡片补齐：新增 `glass-card` / `glass-surface` / `glass-raised` 三档共用玻璃样式（卡片与面板 / 页栏与工具条 / 弹层与对话框），覆盖画布节点面板与各类弹窗、图像工作台与音频合成器的面板与混音条、外壳顶栏与各页面卡片、写作库与工作台，共 129 处；缩放或拖动画布时自动关闭模糊以保住帧率，系统开启「减少透明效果」时自动降级为不透明，玻璃底色跟随浅 / 深主题。
+ [调整] 面板页签不再出现原生滚动条：停靠区页签改为专用容器（隐藏滚动条、支持滚轮横向滚动、激活页签自动滚入视野），页签溢出时右侧出现「全部面板」按钮可直接跳转到任意面板，页签改为品牌色药丸样式，页签条高度仍为 32px。
+ [调整] 写作库与画布库统一为同一形态：左侧分组栏（新建分组、行内重命名、悬停改名 / 删除、选中态用品牌淡底）+ 右侧表格式列表（多选勾选、批量删除与删除全部、拖拽排序、行内打开 / 重命名 / 删除）；写作作品补上分组数据，没有分组的既有作品会自动归入默认分组。
+ [优化] 清理素材库移除后的残留：删除 i18n 中遗留的顶层 `assets` 命名空间，修正 8 处仍指向已删除文案的调用（节点类型名与节点工具栏类型行改走 `canvas.nodeTypes.*`，不再渲染原始 key），并扫净文档中 10 处仍把「我的素材」当成现有功能的描述（quick-start / features / canvas-data-structure / canvas-shortcuts / index / pending-test）。
+ [调整] 顶部导航「我的画布」图标从斜向箭头改为节点 + 连线图标，更贴合节点式画布的定位。
+ [删除] 移除「我的资产」素材库：顶部导航入口与 `/assets` 页面、素材 store 与本地素材数据、选素材弹窗（画布节点与写作立绘都曾用它）、写作资源库条目的立绘字段，以及 MCP 的 `assets_list` / `assets_add` 两个工具（web 侧与 `mcp/src/canvas_mcp` 的 schema / session / instructions 同步删除）全部移除；原本由素材 store 负责的图片与媒体 blob 回收已迁到 `services/image-storage.ts`，删画布项目、删节点时照旧清理孤儿文件。画布上的「资源」节点（绑定本地文件夹）与生成链路不受影响。

+ [新增] 写作工作台补齐专业编辑器的顶部菜单行（与图像工作台、音频合成器同款，共用 studio-chrome 的样式类）：编辑（撤销 / 重做 / 存快照 / 删除节点）、插入（加下级 / 加同级 / 插入资源库条目）、格式（加粗 / 斜体 / 删除线 / 标题 2·3 / 无序·有序列表 / 引用 / 分割线）、视图（专注模式 / 折叠·展开全部大纲）、窗口（面板显隐 + 重置布局），右侧为当前节点与字数状态位；菜单项全部是真实动作（格式类通过 UI store 的命令通道驱动 TipTap，插入类走大纲 store），面板显隐、页签拖动与分隔条改大小沿用同一套 DockArea。实测：菜单按钮 14px / 28px 与另外两个工作台一致，窗口菜单列出 5 个面板，向上拖分隔条底部面板由 208px 增高到 268px。

+ [修复] 修复各页面底部残留一条空白带的问题：布局层的 children 容器写了 `flex-1 min-h-0` 但自身不是 flex 容器，导致内部依赖 `flex-1` 撑高的页面（写作工作台等）只能按内容高度渲染，底部剩下约 85px 仅有页面底色的空带；现在该容器改为 flex 列，实测 studio 的 main 高度从 814px 变为 899px（视口 955 − 顶栏 56），底部停靠区贴到窗口底边。画布编辑器实测无副作用（画布区域本来就撑满，改动后仍撑满）。

+ [调整] 全站配色收敛成一套：选中 / 激活态全站只有一种画法（品牌橙 `theme.node.accent` 作文字 + `accentSoft` 作软底，需要时加 1px 同色环），未选中一律 `theme.node.muted`，主操作用品牌实底。修掉此前三套并存的旧配色：工作台页签（Canvas / Image Studio / Audio studio）是灰底白字、顶部导航被 Ant Design 链接色接管成棕橙（深色下实测 `#d87f36`）、部分工具按钮用同义 token 造成第三种橙（`#9a3412`）。现在四处工作台的激活元素实测 computed 值完全一致（浅色 `#c2410c` / `#fbe9df`，深色 `#fb923c` / `rgba(251,146,60,.16)`），导航与编辑器文字同一套颜色，并把规则写进 `AGENTS.md`（含「`<a>` 导航必须显式覆盖 AntD 链接色」）。

+ [调整] 全站字号重做：面板与控件文本统一到 14px 基线（Ant Design 默认与顶部导航同尺），10px / 11px 一律消灭。四个工作台的面板页签、列表行、字段标签与数值、按钮、菜单项都到 14px，只有密集读数（缩放百分比、时间码、字节数、字数、快捷键提示、88px 窄表头里的数值）保留 12px 下限；写作工作台（大纲行 / 看板卡片 / 检视器 / 资源库 / 历史）、图像工作台（面板 + 图层行 + 属性字段）、音频合成器（表头 / 检视器 / 混音台 / 钢琴卷帘 / 时间轴读数）、画布节点与各面板弹窗、设置各页全部拉齐。实测：面板页签 14px 且无截断，整屏不存在低于 12px 的文本，控件高度统一 28px。
+ [修复] 修复放大字号后暴露出的三处截断：「Adjustments」面板页签被截成「Adjustmen…」（页签标签上限 72px → 96px）、图层面板头的「Layers」被截断（该标题与页签重复，降为 12px）、右侧最后一个「Actions」页签顶到窗口边缘（页签内边距收紧）；并顺手把画布拖拽浮标里的 9px 文字提到 12px 下限。

+ [调整] 统一四个工作台顶部选项行的文字样式：`Select / Edit / Image / Filter / View` 是 12px 弱化色、`Window` 也是 12px 弱化色、当前工具名却是 `theme.node.text` + font-medium，同一行里大小与深浅不一；现在统一为 13px、`text-foreground`、字重 500，图像工作台 / 音频合成器 / 文本创作器共用同一份 `STUDIO_MENU_BUTTON_CLASS`，改一处四端同步。
+ [调整] 面板底色不再「死白」并统一四个工作台：浅色页底由 #f2efe9 改为 #eeeae3、面板/卡片由 #fdfcfa 改为 #f8f6f1，深色页底 #1a1815、面板 #242119，页面与面板之间仍有层次但整体转为暖调纸面；正文对比度 16.2:1、辅助文字 7.1:1、最弱文字 5.3:1（全部达 AA/AAA）。配色走 `canvasThemes` + CSS 变量，画布、图像工作台、音频合成器、文本创作器一处生效。

+ [修复] 修复图像工作台里画板图层「显示不全、只剩左上角一块」的问题：智能画板节点的 width/height 同时充当画布卡片尺寸和画板文档坐标系，在画布上缩放该节点会连带把文档坐标系改小（你的画板从 640×360 变成 284×160），而图层仍停在原坐标系里，超出部分被舞台裁掉；现在打开图像工作台时会检测「图层内容超出画板」并把文档尺寸还原成按比例推导的基准尺寸。该检测只在内容确实溢出时触发，不会影响「图像大小 / 画布大小」这类合法改尺寸操作（那类操作会同步重采样图层）。

+ [修复] 修复停靠面板底部区域拖拽方向相反的问题：底部停靠区的分隔条位于面板上方，向上拖动本应增高面板，原先却按向下计算导致反向收缩（图像工作台、音频合成器、文本创作器的底部面板都受影响）。
+ [修复] 修复图像工作台画板显示不全 / 偏移的问题：视图原先只在首次测量容器时自动适配一次，之后容器尺寸变化（切换工作台、拖动停靠分隔条、改变窗口大小）不再重新适配，画板就会偏移出可视区域；现在只要用户还没手动平移或缩放，容器尺寸变化都会重新居中适配，用户一旦手动操作就保留其视图。
+ [调整] 交互反馈色统一到主题：把 49 个文件里 130 处写死的 `hover:bg-black/5 dark:hover:bg-white/10`（以及 group-hover / focus / before 变体）收敛为主题 token `hover:bg-hover`（浅色为品牌暖调、深色为提亮），并在 `AGENTS.md` 写明「颜色一律走 token、只有压在图片上的内容物才允许写死黑白」，避免后续再写死黑白透明度。
+ [调整] 全局视觉升级：重做浅色 / 深色配色并接入 Ant Design token —— 正文对比度 15.2:1、次要文字 9.0:1、辅助文字 6.7:1、最弱文字 5.0:1（原先辅助文字仅 4.3:1、最弱文字 2.3:1，均不达 WCAG AA），深色主题同步提升（辅助 10.2:1、最弱 7.2:1）；引入品牌陶土橙主色（浅色 #c2410c / 深色 #fb923c）替换原来的近黑主色，并新增 成功 / 警告 / 危险 / 提示 四组语义色与各自的淡底变体，用于主按钮、链接、激活页签、选中行、状态标签与焦点态。
+ [调整] 全站文字可读性：清掉 94 处把文字压到约 1.8–3:1 的 `opacity-40/50/60/70` 虚化（保留禁用态与纯装饰的透明度），最小字号从 10px 抬到 11px 以上、正文与列表正文抬到 13–15px，并逐页提升画布节点与元信息条 / 连线标签 / 节点面板与弹窗、画布库与资源页、设置各页、图像工作台面板与停靠页签、音频合成器表头 · 混音台 · 时间轴标签、文本创作器（大纲 / 正文 / 资源库 / 检视器 / 看板 / 历史）的对比度与状态色。
+ [新增] 新增顶层「文本创作器」工作台（`/write`，与画布 / 我的资产 / 配置同级）：作品库（新建 / 重命名 / 删除，内置小说与剧本两套大纲预设，正文按写作单元保存——小说按章、剧本按场）＋ 章节管理器（拖拽大纲树、状态、字数、AI 展开下级）＋ TipTap 块式正文编辑器（自动保存、`/` 斜杠菜单、划词 AI 续写 / 改写 / 扩写 / 精简 / 润色 / 写对话，流式输出可采纳或丢弃）＋ 作品内资源库（人物 / 地点 / 设定 / 道具 / 线索）＋ 检视器 ＋ 场景看板（拖拽改状态与顺序）＋ 版本历史（快照 / 回滚），面板走可停靠布局（窗口菜单显隐、页签可在左 / 右 / 底之间拖动、布局按工作台记忆），支持导出 Fountain。
+ [删除] 移除画布的写作节点：底部工具栏的「文字」组、故事节点（前提 / 幕 / 章 / 场景 / 节拍 / 情节点 / 人物 / 地点 / 传说 / 线索）、故事流 / 角色弧光 / 场景设定 / 主题 / 线索 等连线关系与 `expand_story` 智能体操作全部删除，写作能力迁移到「文本创作器」工作台。
+ [修复] 修复音频工作台走带行与选项栏控件高度不一致、选项栏在窄屏下被截断的问题：两行控件统一为 28px 并统一圆角 / 边框 / 内边距，拍号下拉不再比旁边的速度输入更高，选项栏控件在窄屏下改为收缩换行而不是被裁切，两行高度保持 44px / 36px 不变。
+ [修复] 修复音频工作台轨道表头控制行列错位的问题：表头第二行改为固定列槽位、缺失的控件保留占位，静音 / 独奏 / 录音待命指示 / 增益输入在所有轨道类型（音频 / 乐器 / MIDI / 分组 / 返回 / 主输出及增益自动化行）上严格对齐，主输出行的增益输入不再比其它行偏左，行高 68px 与表头列 / 片段行的逐行对齐保持不变。
+ [调整] 音频工作台除混音台通道条自己的竖直推子外，所有音量 / 增益滑杆改为紧凑的数值输入框（轨道表头、检查器、发送增益、片段增益、录音输入增益、节拍器音量与工程总音量）：显示沿用原推子的格式（静音为「-∞ dB」、单位增益为「0.0 dB」、正向为「+3.0 dB」），输入过程中可以正常输入 -、+ 与小数点，回车或失焦提交并按原滑杆范围夹取，双击输入框回到该参数的默认值。
+ [调整] 音频工作台的标尺与播放头支持按住拖动连续改变播放位置：在标尺整个高度上按下（含播放头）左右拖动即可刷新播放时间，播放中保持播放并跟随指针，位置只写入传输时钟和播放头元素（不经过文档，因此不会逐帧重渲染工作台），单击标尺仍然一步跳转，标尺带左右拖动光标与悬停反馈。
+ [修复] 修复音频工作台轨道表头与轨道行高度不一致的问题：轨道行高按表头内容固定为 68px，表头列与片段行严格逐行对齐、不再互相遮挡（「添加轨道」行与主输出行也包含在内），片段 / MIDI 片段与波形始终正常绘制；未命名的主输出 / 分组 / 返回轨改为按角色显示，不再被编号成轨道（原来新建工程会显示成「Master Track 2」）。
+ [修复] 修复音频工作台推子只能衰减不能提升的问题：推子范围从 -60 dB…0 dB 改为 -60 dB…+6 dB（底部表示静音、0.0 dB 为单位增益），轨道 / 发送 / 主输出增益与增益自动化轨的取值上限同步放宽到 +6 dB，共享音频图构建器按线性增益应用（+6 dB 在离线导出中实测放大 1.995 倍），正向数值显示为「+3.0 dB」。
+ [优化] 音频工作台的拖动改为手势期间只写被拖动元素的样式、松手时一次性写入文档：片段移动 / 裁剪 / 淡入淡出 / 循环、MIDI 片段移动 / 裁剪、自动化断点与框选都不再每帧重渲染整个工作台（40 步拖动的帧间隔 p95 由 58ms 降到 8.5ms，DOM 变更由 2044 次降到 266 次），一次拖动仍只产生一次撤销。
+ [新增] 图像工作台与音频工作台的面板改为可停靠面板系统：所有面板由选项栏新增的「窗口」菜单统一显示 / 隐藏并支持一键重置布局，拖动面板页签可在左 / 右 / 底三个停靠区之间移动（同一停靠区里的多个面板以页签堆叠），拖动分隔条可调整停靠区宽度或高度，布局按工作台分别记录在本地并在刷新后保留。
+ [新增] 画布支持把「音频」节点拖到「音频合成器」节点上：松手后为每个被拖入的音频节点在工程里新建一条轨道并追加一个引用该节点的片段（命名、时长与起点沿用工作台「添加片段」的规则），节点动画回弹到拖动前的位置，整次拖放只产生一次撤销。
+ [调整] 智能画板节点在画布上改为纯信息卡片：只显示图标、比例 · 分辨率与图层数，不再实时渲染图层、嵌套画板与文本（图层文档仍只在图像工作台与合成导出中渲染），拖放行为不变。
+ [调整] 移除画布节点面板里的「参考内容」参考条：节点面板不再显示参考缩略图、添加参考与断开参考入口，@ 引用与连线参考仍照常参与生成。
+ [调整] 音频合成器节点从底部工具栏的输入/输出菜单移到顶栏，作为一个独立按钮放在「智能画布」按钮前面；英文名由 Audio project 改为 Audio Studio（中文名本来就是「音频合成器」，未变）。
+ [修复] 音频工作台在没有选中音频合成器节点时会自动打开画布上的第一个，和图像工作台的行为一致，不再需要先回画布手动选中。
+ [新增] 画布「音频合成器」工作区升级为 Cubase 式多轨 DAW 的第六阶段（录音与导出完善）：走带上的录音按钮从「切换录音待命」改为真正录音（需先让音频轨道进入待命），点击后在同一个手势里解锁音频上下文并打开默认输入，按工程设置里的预备拍（0 / 1 / 2 / 4 小节，按拍号在小节首重音、不占用录制内容）倒数后从待命轨道上开始录音，再次点击或用空格 / 暂停 / 停止结束，穿插录音模式只在已启用的穿插范围内采集并在出点自动收尾；工程设置新增录音模式、声道（单声道会下混）、输入增益与输入延迟（把成片整体前移以补偿输入延迟），录音期间输入经该轨道的通道条监听（受其静音 / 独奏门控）；每次录音在结束时解码并以 16 位 WAV 写回画布（新音频节点）与轨道片段，覆盖范围内的原片段按新录音裁剪，整次录音只产生一次撤销，音频字节仍不进入文档；节拍器改为真正发声并在工程设置里调音量，且只有播放 / 录音时出声、不进入离线混音导出；导出侧补齐状态与分轨：「存为音频节点」在导出中显示「导出中…」并禁用，「轨道」菜单新增「导出分轨」，对每条轨道用同一个共享音频图构建器离线渲染一条分轨（音频 / 乐器 / MIDI 轨渲染自身内容，分组轨汇总路由进它的轨道）并一次生成多个音频节点。
+ [新增] 画布「音频合成器」工作区升级为 Cubase 式多轨 DAW 的第五阶段（MIDI 与乐器轨）：轨道新增「乐器 / MIDI」类型（各自自带合成器，乐器轨可选音色），MIDI 片段按 PPQN 刻度（每四分音符 960 ticks，跟随工程速度）记录，可在编曲区移动、两端裁剪、分割、复制与删除，双击片段或用中间视图新增的「钢琴卷帘」进入基础钢琴卷帘编辑器（双击空白处画音符、拖动移动、拖右边缘改长度、下方力度区拖动力度、双击或 Delete 删除、方向键微调、Shift 按小节 / 八度），合成器与音频片段一样接入同一条通道条（推子 / 声像 / 静音 / 独奏 / 发送 / 输出路由全部生效），播放与离线混音导出仍共用同一个音频图构建器因而导出的 WAV 同样包含 MIDI 轨。
+ [新增] 画布「音频合成器」工作区升级为 Cubase 式多轨 DAW 的第四阶段（波形与性能 + 窄屏适配与无障碍）：片段波形改为多分辨率峰值金字塔并按缩放选择层级、由每个片段自己的 canvas 只绘制滚动可见窗口（峰值与解码 PCM 走带 LRU 上限的内存缓存，播放与波形共用同一次解码），编曲视图按滚动视口虚拟化轨道行；音频图改为首次手势解锁音频上下文后才构建，首次播放会等待播放器就绪；文档变更拆分为结构键与混音键，拖动推子或编辑自动化点不再重建播放器，自动化改为在实时音频图上重新调度；窄屏下右侧面板收为由顶栏按钮打开的浮层（打开即获焦、点击外部或 Esc 关闭、Tab 不越出面板），工具列变横向条，走带与选项收进「更多 / 选项」下拉，混音台一次显示一条通道条；并补齐 focus-visible 焦点轮廓、可聚焦且可用方向键微调的自动化点、聚焦片段回车打开检查器等无障碍细节。
+ [新增] 画布「音频合成器」工作区升级为 Cubase 式多轨 DAW 的第三阶段（参数自动化）：轨道可对音量 / 声像 / 发送增益建立自动化轨，自动化轨与时间轴共用水平比例并以折线断点编辑（双击空白处添加、拖动移动、双击点删除、右键在 线性 / 保持 / S 曲线 之间切换插值），轨道表头新增折叠自动化轨的箭头，「自动化」菜单与右侧「自动化」面板从占位改为可用（添加 / 启用 / 清除 / 删除自动化并显示点数与手势提示）；启用且含断点的自动化轨会接管对应参数，由共享音频图构建器在传输时钟上调度断点（保持为阶跃、S 曲线用多段线性近似），并在传输启动与循环回跳时重建以免残留旧渐变，离线混音导出走同一构建器因此导出的 WAV 同样带自动化；被接管的参数在混音台通道条、检查器与轨道表头显示链接图标且只读，手动推子不会再覆盖自动化，删除轨道或发送会清理指向它的自动化轨。
+ [新增] 画布「音频合成器」工作区升级为 Cubase 式多轨 DAW 的第二阶段（混音台、路由与总线）：新增「编曲 / 混音」视图切换与混音台（每轨颜色条、发送槽位、声像、dB 推子与立体声电平表、静音 / 独奏 / 录音待命、输出选择，主输出推子固定在右端并绑定总音量），轨道新增 音频 / 分组 / 返回 / 主输出 类型与逐轨输出路由（拒绝成环）、只能指向返回轨的前 / 后推子辅助发送、按路由图解析的全局独奏 / 静音，右侧补齐轨道检查器、媒体池与工程设置（速度 / 拍号 / 网格 / 循环 / 穿插录音 / 节拍器 / 总音量）面板，播放与离线导出改为共用同一个音频图构建器以保证导出一致。
+ [新增] 画布「音频合成器」工作区升级为 Cubase 式多轨 DAW 的第一阶段（音乐时间轴与片段编辑）：标尺改为按速度与拍号显示小节 / 拍并加入网格与吸附、缩放滚动、标记与循环区域，片段支持多选、移动、非破坏裁剪、分割、复制粘贴、淡入淡出与自动交叉淡化、循环、反转、增益、锁定 / 静音 / 颜色 / 重命名，并补齐左侧工具列、顶部编曲 / 轨道 / 片段 / 视图 / 自动化菜单与随工具变化的选项栏、右侧检查器 / 自动化 / 历史与媒体池 / 标记面板栏；音频文档仍保存在音频合成器节点元数据中、片段只引用画布音频节点，混音导出路径不变。
+ [调整] 画布「图像合成器」工作区改名为「图像工作台」（Image Studio）：组件与文件同步改名，路由段仍走 `/canvas/:id/image`。
+ [新增] 画布「图像工作台」补齐素材面板与进阶编辑能力：右侧改为分页签面板，新增 Photoshop 式取色器与色板 / 渐变 / 图案 / 画笔面板（预设通过 localforage 持久化，图案位图存入图片存储并纳入清理扫描）；「自由变换」新增斜切、扭曲、透视与 4×4 网格变形，并提供数值变换与「重复上一次变换」，变换数据保存在图层上、由共享栅格渲染器按网格细分绘制；文字图层新增字符 / 段落面板（字距、字偶间距、行距、基线偏移、水平垂直缩放、仿粗体仿斜体、下划线与删除线、大小写、对齐与缩进、连字）以及沿路径文字与文字变形；新增通道面板（RGB / 单通道查看、通道显隐、把选区存为具名 Alpha 通道并载入 / 重命名 / 删除）；新增路径面板与钢笔、直接选择工具（可建立选区、填充与描边路径）；新增历史记录面板（可点击回退、快照、上限 50 条，仅存在于编辑会话）与动作面板（录制菜单 / 图层 / 调整 / 滤镜 / 变换等文档命令，可保存、播放、重命名与删除）。

+ [新增] 画布「图像工作台」新增调整图层、滤镜与图层样式：图层文档支持带参数的调整图层（亮度/对比度、色阶、曲线、曝光、自然饱和度、色相/饱和度、色彩平衡、黑白、照片滤镜、通道混合器、渐变映射、反相、色调分离、阈值、可选颜色），按 Photoshop 语义只影响同容器内其下方的图层，并可设不透明度、混合模式与蒙版；「滤镜」菜单提供 22 个破坏性滤镜（模糊、锐化、杂色、像素化、风格化、扭曲六组，含倾斜 / 光圈模糊与液化），每个都有参数对话框与实时预览，文字 / 形状 / 图片 / 组图层会先栅格化，结果按图层蒙版与选区裁剪，一次应用一次撤销，并支持「重复上一次滤镜」；图层可启用图层样式（描边、投影、内阴影、外发光、内发光、斜面和浮雕、光泽、颜色 / 渐变 / 图案叠加，投影与外发光在内容下方、其余在上方），图层面板底部与双击图层行均可打开样式面板；合成、导出与预览改用同一个共享栅格渲染器 `renderPsDocument`，调整图层与图层样式的预览与「存为图片节点」导出在结构上一致，未使用调整或样式的文档仍走原有 DOM 渲染。
+ [新增] 画布「图像工作台」新增选区、图层蒙版、渐变与形状工具、「图像」菜单与标尺 / 网格 / 参考线 / 对齐：矩形 / 椭圆选框、套索、多边形套索、魔棒与快速选择创建带动态虚线的选区（只存在编辑器状态），画笔 / 橡皮擦 / 油漆桶 / 渐变的每次绘制都被选区裁剪；图层可添加 / 删除 / 应用蒙版（白显示黑隐藏，选中蒙版后绘制改为编辑蒙版，画布预览与合成导出一致）；渐变与矩形 / 圆角矩形 / 椭圆 / 多边形 / 直线作为新的 `shape` 图层类型（预览用 SVG、导出用同一个 `Path2D`）；「图像」菜单支持图像大小 / 画布大小 / 旋转 / 裁剪到选区 / 修剪边框并同步画板节点尺寸与比例；「视图」菜单可开关标尺、网格、参考线与移动 / 缩放吸附。
+ [新增] 画布「图像工作台」新增像素图层与绘制工具：图层文档在图片 / 文字 / 组之外支持自带位图的像素图层，位图通过图片存储以图层自己的 `storageKey` 保存（不写进节点 metadata，并会被图片清理扫描当作在用资源保留）；工具栏新增画笔 / 橡皮擦 / 油漆桶 / 吸管（快捷键 B / E / G / I，[ / ] 调整笔刷大小，原有的移动、抓手与缩放不变），顶部新增随当前工具切换的选项栏（大小、硬度、不透明度、容差与前景色），画笔与橡皮擦按大小、硬度、不透明度连续绘制并带跟随光标的笔刷圆环，油漆桶按容差做 4 邻域填充，吸管从合成结果取色；绘制只写入当前像素图层，当前图层不是像素图层时自动新建一个覆盖整个画板的像素图层，一次拖动（或一次填充）只在松开时写回一次 `boardLayers`，所以一次撤销即可还原整笔，画板预览与「存为图片节点」导出同样按图层的混合模式、不透明度与旋转渲染像素图层。
+ [新增] 画布「图像工作台」工作区从占位改为 Photoshop 式图层编辑器：顶部选项栏（画板切换、比例 / 分辨率 / 背景设置、缩放读数与「存为图片节点」输出）、左侧工具栏（移动 / 抓手与缩放）、中间可缩放平移并带选区框与缩放、旋转手柄的画布文档、右侧「图层 / 属性」面板；支持从画布图片节点添加图片层、添加文字层（画布内双击改字）、图层重命名 / 复制 / 删除 / 上下移与拖拽排序、不透明度与混合模式、单级编组（组内图层参与画布预览与合成导出），所有改动都写回画板节点的 `boardLayers`，撤销、自动保存与「存为图片节点」导出照旧。
+ [调整] 智能画板改为自包含图层文档：图片层 / 文字层的位置、尺寸、旋转、不透明度、混合模式与显隐都保存在画板节点自己的 `boardLayers` 里，不再用画布节点的空间位置或 `metadata.boardId` 表示板内成员；把图片或画板节点拖到画板上会按原始比例居中追加图层，并把节点动画回弹到拖动前的位置（节点仍是普通可拖拽节点）；画板预览改为只读（背景、网格、图层与嵌套画板递归），画板面板只保留设置与「存为图片节点」。
+ [调整] 图像工作台不再需要先在画布上选中智能画板：没有选中时自动打开第一个画板，顶部新增画板下拉框可在项目的所有智能画板之间切换。
+ [修复] 修复切换工作区 tab 后返回画布整块空白、需要刷新才恢复的问题：画布项目的加载 effect 不再因路由变化（如 tab）重跑，只在项目真正切换时才重新加载。
+ [修复] 修复切换工作区 tab 后返回画布节点全部不可见（需刷新才恢复）的问题：画布容器在工作区切换时会卸载、旧版 ResizeObserver 会把画布尺寸写成 0，现改为工作区变化时重新测量并观察新容器。
+ [新增] 画布「音频合成器」工作区从占位改为多轨音频编辑器：新增「音频合成器」节点作为音频文件（轨道、片段与总音量都存在该节点元数据里，片段只引用画布上已有的音频节点、不复制音频数据），工作区提供逐轨片段波形与共享时间轴、播放/暂停/停止与拖拽定位、逐轨名称/音量/静音/独奏与总音量、把音频节点添加为片段并跨轨拖动，以及用 Tone.js 离线渲染导出 WAV 混音并存为新的音频节点。
+ [新增] 画布页面新增「画布 / 图像工作台 / 音频合成器」三个工作区切换：图像工作台与音频合成器分别以智能画板节点、音频合成器节点为文件，在节点自身的元数据上编辑并输出为新的画布节点。
+ [调整] 画布底部工具栏的创建分组菜单顺序调整为「提示词、修改器、生成器」。
+ [修复] 修复画布底部工具栏的按钮提示与二级菜单重叠的问题：打开创建菜单、扩展或缩放面板时不再显示按钮提示。
+ [新增] 画布「资源」节点列表支持按需预览：悬停任意文件行会显示预览按钮，点击后在弹窗里查看该文件的图片、视频播放器、音频播放器或文本内容；列表仍然只显示文件名，只有正在预览的那个文件才会被读取，其他文件不会被预加载。
+ [调整] 拖动画布「资源」节点（图片 / 视频 / 音频）时改为在光标处显示小型拖拽浮标并把真实节点淡显，不再让全尺寸节点挡住资源节点、图片修饰节点与视频提示词槽位等拖放目标；非资源节点拖动、节点几何与命中判定均保持不变。
+ [调整] 画布「资源」节点的本地文件夹改为文件夹浏览器：绑定的文件夹作为根目录，可进入子文件夹，并通过返回按钮与面包屑逐级回退、无法超出根目录；导航路径只保存在内存中，重新打开画布或切换画布后回到根目录。
+ [调整] 画布「资源」节点左上角去掉重复的节点名称，改为来源下拉框（本地文件夹 / 插件缓存），来源选择与绑定、刷新并到同一行。
+ [调整] 画布底部工具栏在「修改器」与「智能画布」之间新增分割线，把四个创建分组菜单（输入输出、提示词、生成器、修改器）与「智能画布」按钮分开。

+ [调整] 清理已移除功能留下的死代码与残留数据：删除无引用的本地工作区同步 store/模块与 output-resolution 辅助函数；移除已删除 Agent 对话在画布项目、撤销历史和导出 zip 中的字段（chatSessions / activeChatId、助手图片水合、`agent_chat_messages` 存储标签）；删除未使用的 i18n 文案与冗余导出。同时补齐「音频」素材类型与「未命名画布」两处缺失文案，修复界面显示原始 key 的问题。
+ [修复] 修复 MiniMax 视频模型同时发送首帧和尾帧导致接口报错的问题：`minimax/hailuo-3` 与 `minimax/hailuo-3-max` 按模型能力只允许 1 张关键帧，「视频提示词」节点在首尾帧模式下改为单个「关键帧」槽位并可在首帧 / 尾帧之间切换（切换会替换已绑定的另一种帧），发送前按模型上限截断 `frame_images`，旧画布即使仍同时保存首帧与尾帧也不会再被拒绝；允许两张帧的模型保留原有双槽位。
+ [新增] 新增浏览器扩展 `extension/`（Chrome/Edge MV3）：右键任意网页图片「添加到 OpenCanvas 缓存」，图片存入扩展本地 IndexedDB（名称、类型、尺寸、原页面与原始字节），再通过 content script + `postMessage` 桥接把缓存列表与图片字节提供给本地 OpenCanvas；无后端、不上传，未安装插件时不影响原有功能。
+ [调整] 画布 MCP 指令与工具描述同步当前能力：补齐视频生成（`minimax/hailuo-3-max`、480p/768p、5-15 秒、frames/reference、`videoSlots`）、智能画布（嵌套画板、图层顺序与隐藏、16 种混合模式与不透明度、排版模板、文本标注）与资源节点（单一文件夹浏览器 + 插件缓存来源）。

+ [新增] 画布「资源」节点新增「插件缓存」来源：节点顶部可在「本地文件夹」与「插件缓存」之间切换（选择随节点 metadata 保存），切到插件缓存后列出插件缓存里的图片文件名，可拖到画布或点击插入为普通图片节点，未安装插件时显示「未检测到插件」提示且不影响本地文件夹的原有行为。
+ [调整] 视频生成新增 OpenRouter 原生接口：`minimax/hailuo-3-max`（MiniMax: H3 Max）走 OpenRouter 专用异步视频接口（提交任务、按 polling_url 轮询、带鉴权下载，费用读取真实 `usage.cost`），视频参数面板按模型能力只展示 480p/768p、5-15 秒与六种宽高比，首尾帧与参考图分别映射为 `frame_images` / `input_references`，不再发送 `size`、`generate_audio`、`watermark` 与 `seed`；插件脚本与旧 OpenAI 兼容视频分支行为不变。
+ [调整] 视频生成参考素材按模型原生能力重做：新增旗舰模型 `minimax/hailuo-3`（2K、原生音频、支持参考素材）并保留 `minimax/hailuo-3-max`（480p/768p、无音频）为默认；「视频提示词」节点分「首尾帧 / 参考素材」两种模式，参考素材槽位可混拖图片/视频/音频节点（图片 ≤9、视频 ≤3、音频 ≤3、合计 ≤12，参考音频不能单独使用），请求按 OpenRouter 原生字段发送带显式 `frame_type` 的 `frame_images` 与图片/视频/音频混合的 `input_references`，仅支持音频的模型才发送 `generate_audio`（不再发送 `size` 与 `seed`），本地视频/音频参考会从浏览器存储读出真实字节转成 data URL，超过 50MB / 15MB / 64MB 上限时明确报错。
+ [调整] 画布「资源」节点改为单一文件夹浏览器：每个节点各自绑定并单独记住一个本地文件夹（按节点保存在 IndexedDB，新建节点默认不绑定，不再共享全局文件夹；绑定不会写入节点 metadata 或画布项目数据），节点内改为只显示文件名的纵向列表（不再显示缩略图或网格），把文件拖到画布或点击仍会插入对应节点；移除节点的输出文件夹与上游自动写入，保留把图片节点拖到资源节点上收集进该节点文件夹的行为。
+ [调整] 画布 MCP 补全节点类型：`canvas_create_node` 与 `canvas_apply_ops` 的 `add_node` 现在支持全部内置节点类型（文本、提示词、音乐提示词、语音提示词、图片、视频、音频、生成配置、图片生成、语音生成、音乐生成、智能画布、资源、录音、图片修饰），`delete_node` 支持按 `nodeType` 删除；同步更新 MCP 指令与文档里的节点类型和工具数量（33 个）。
+ [调整] README 与功能介绍按当前实现重写：README 移除过期截图与失效链接，收敛为项目介绍、核心功能、快速开始、配置说明与文档入口；功能介绍中英文版本同步更新到当前的节点、生成、图片工具、智能画布、素材与 MCP 能力。
+ [调整] 画布新增「视频提示词」与「视频生成」节点：底部工具栏提示词菜单加入「视频提示词」、生成器菜单加入「视频生成」、输入/输出菜单加入「视频」媒体节点；视频提示词连到视频生成节点后为其提供提示词与参考素材，视频生成节点内联模型和视频参数设置并复用现有视频生成流程，同时补齐 MCP 内置节点类型列表。
+ [调整] 视频节点套件重做：「视频」媒体节点从底部工具栏输入/输出菜单与连线创建菜单移除，只能从资源节点拖入文件或由视频生成产生；「视频提示词」节点独占首尾帧 / 参考图模式，并提供可拖入图片节点的槽位（首帧、尾帧或按顺序填充的参考图，槽位悬停高亮、填充后显示缩略图并可一键清除，松手后图片回弹原位）；「视频生成」节点把模型、清晰度、宽高比与时长内联展示（去掉设置弹层与手动 W/H），生成时读取提示词节点的模式与槽位图片并按顺序作为首尾帧或参考图发送。

+ [新增] 画布新增整包导出与导入：底部工具栏最左侧「导出画布」把当前画布的节点、连线、视图、对话与全部图片/视频/音频资源打包成一个 zip 下载，包内用 `projects.json` 清单记录格式版本、库名与每个资源对应的文件；画布库与左侧画布切换器新增「载入」，可拖入或选择该 zip 新建画布（不覆盖已有画布），恢复节点/连线/视图并把资源写回本地存储，库名同名则复用、不存在则新建。
+ [调整] 画布左侧面板画布切换器移除标题搜索框，表头改为一行「当前库名 + 新建 + 载入」；画布库页面的导入入口改为同一个导入弹窗。

+ [新增] 画布「输出」节点新增输出文件夹：节点内可绑定一个本地文件夹（File System Access API，需读写权限），绑定后该输出节点每次解析到新的图片、视频或音频输出时都会自动写入该文件夹，文件名取输出来源节点标题并清理非法字符、扩展名按媒体类型推断；重复的同一份输出不会重复写入，写入失败只在节点内提示而不阻断画布。
+ [新增] 画布区域新增顶部与左侧坐标标尺：随平移、缩放实时更新刻度，缩小到一定程度自动拉大刻度间距，且不拦截鼠标操作。
+ [新增] 画布拖动节点新增 16px 网格吸附：与其他节点对齐的参考线优先，无参考线时按 16px 网格对齐；吸附按画布坐标计算，缩放时手感一致。
+ [新增] 多选节点时，选择浮层新增对齐与分布操作：左/水平居中/右、上/垂直居中/下对齐，以及水平/垂直等距分布（分布需选中 3 个以上节点），一次操作可整体撤销。
+ [新增] 画布左侧面板顶部新增画布快速切换：可按标题搜索、显示每个画布的节点数量、高亮当前画布，选中后直接跳转，不需要先返回画布库。
+ [新增] 画布新增节点锁定与隐藏：锁定后节点不可拖动、缩放、编辑或删除，隐藏后不在画布渲染但仍可在「节点列表」中恢复。
+ [新增] 画布底部工具栏新增「节点列表」：可按类型筛选画布节点，多选后批量重命名，并直接切换锁定/隐藏；节点悬停工具栏的「层级」弹层复用同一列表。
+ [新增] 画布生成新增执行层：所有生图请求统一经调度器排队执行（并发上限 2，排队中的图片仍显示为等待中），失败自动重试最多 3 次、每次间隔 2 秒；每个生成配置节点只保留最近 5 条结果版本，并记录本次生图使用的随机种子，支持「同种子重播」按原种子重新发起请求以复现结果。
+ [新增] 生成配置节点新增「参数矩阵」：尺寸、数量、提示词可各填一小组取值（逗号或换行分隔），一次生成按现有队列把全部组合逐个入队，生成按钮与矩阵入口实时显示将生成的变体数量。
+ [新增] 提示词支持变量：生成配置节点与节点提示词面板新增「变量」入口，自动列出提示词里的 {{变量}} 并可逐项填值、实时预览替换结果，生成时用填写的值替换提示词，未匹配的写法保持原样。
+ [新增] 智能画布新增布局模板与板内图层：一键排版改为下拉菜单，可选网格 / 单行 / 单列 / 主图四种模板；画板面板新增图层入口，可查看画板上图片的前后顺序、上移或下移一层，并单独显示或隐藏某张图片，画板渲染与合成顺序都与图层一致。
+ [新增] 画布文本节点新增文字样式设置：悬停工具栏的「文字样式」可设置字体、字号、行高、加粗、斜体、左/中/右对齐与文字颜色，样式随节点保存并在画布渲染时生效，颜色未设置时跟随画布主题。
+ [新增] 智能画布支持嵌套：可把画板拖到另一个画板上成为板内图层，父画板递归渲染嵌套画板内容，一键合成也会递归合成嵌套画板（按图层顺序、受嵌套画板矩形裁剪）；拖动画板时其板内图层（图片与嵌套画板）整棵子树跟随移动，画板不能拖入自身或其子孙，出现循环时渲染与合成都直接跳过而不是死循环。
+ [新增] 画布 MCP 新增节点工具：设置锁定/隐藏（canvas_set_node_flags）、批量重命名（canvas_bulk_rename，多选按顺序编号）、对齐与等距分布（canvas_align_nodes）与复制节点（canvas_duplicate_node）；节点 metadata 支持显式传 null 清除字段。
+ [新增] 画布 Agent 新增操作审计与权限：设置新增「Agent」页签，可逐类开关 10 种画布操作权限（默认全部允许、行为不变），被拦截的操作会弹出提示并计入审计；每次 Agent 操作记录审计日志（时间、操作数、被拦截数与操作摘要，最多保留最近 100 条），支持按记录一键回放并在新记录上标记来源。
+ [新增] 画布新增「输出」节点：只读展示其直接上游输入中最晚更新的那个（按节点更新时间判定，无更新记录时退回最后一条连线顺序），并按输入类型展示图片 / 视频 / 音频 / 文本；存在多个输出节点时以最早创建的为默认输出，所有输出节点都会显示默认输出来源提示。
+ [新增] 设置新增「工作区」页签：可绑定一个本地文件夹（File System Access API）并在浏览器与文件夹之间双向同步画布项目；每次同步按改动时间判断上传或下载，双方都有改动时逐个询问保留哪一边，未选择的一边会另存为 `_conflict` 副本，两边都会保留在文件夹里。
+ [新增] 图片节点新增「图片分析」：悬停工具栏可打开分析弹窗，展示图片主色板、EXIF 信息与感知哈希，并可按所选比例用智能裁剪生成新的裁剪图片节点。
+ [新增] 图片节点新增「识别文字」：悬停工具栏可调用已配置的文本模型识别图片中的全部可见文字，并在右侧生成、选中一个包含识别结果的文本节点；未配置文本模型时会打开设置，未识别到文字会给出提示。
+ [新增] 智能画布图层面板新增混合模式与不透明度：每个板内图层（图片或嵌套画板）可选择 16 种 Photoshop 等效混合模式并单独调整不透明度，画布预览用 CSS `mix-blend-mode`、合成导出用 canvas `globalCompositeOperation`，预览与导出结果一一对应。
+ [新增] 图片节点新增「分割」：悬停工具栏可打开点选分割弹窗，在图片上左键点前景、Alt 或右键点背景，浏览器本地运行 MobileSAM（ONNX 编码器/解码器，`onnxruntime-web` 推理，首次按需下载约 45MB 并缓存），生成透明底的分割图并另存为新图片节点，弹窗显示所选蒙版的 IoU 与叠加预览；分割模型已加入设置里的本地模型列表，可单独下载或删除。
+ [调整] 画布「输出」节点与「资源输入」节点合并为一个「资源」节点：同一个节点既可绑定本地文件夹平铺展示文件、拖拽缩略图到画布，也可绑定输出文件夹自动写入上游生成结果，文件夹句柄统一由一个工作区 store 管理；底部工具栏与右键创建菜单不再提供独立的输出节点，节点类型改为 `assets`。

+ [新增] 画布新增「资源输入」节点：可绑定一个本地文件夹（File System Access API，句柄单独保存在 IndexedDB 中），节点内以缩略图平铺展示文件夹里的图片、视频、音频与文本，把缩略图拖到画布上即可创建对应节点，也可以点击在画布中心插入；该节点不能连线，重新打开画布会自动恢复绑定与文件列表。
+ [调整] 画布左侧面板移除「素材」页签，只保留画布列表；资产库改由画布上的「资源输入」节点承载，独立「我的资产」页面与资产选择弹窗保持不变。

+ [调整] 提示词节点改为纯提示词节点：节点只展示提示词文本与面板提示，下方面板顶部提供「从预设库选择」入口（弹窗浏览预设库），下方为提示词编辑框，节点可作为提示词来源连接到生成节点。

+ [调整] 产品更名为 OpenCanvas：前端文案、页面标题、包名（`opencanvas`）、MCP 服务名（`opencanvas-mcp`）与全部文档统一更新，本地存储、导出格式与 MCP 工具名保持不变。

+ [优化] 画布拖动改为只更新被拖动节点的位移预览，拖动帧不再重渲染全部节点；图片节点在画布上统一改用最长边 512px 的 WebP 缩略图渲染，只有查看大图时才加载原图，并在首次渲染时懒生成旧图片的缩略图。
+ [优化] 统一画布浮层动效：图片/视频/音频设置、智能画布设置、图层面板与文字样式弹层统一使用同一进入动画，并在系统开启「减少动态效果」时自动关闭。
+ [调整] 移除画布底部工具栏的「清空画布」按钮及其确认弹窗。
+ [调整] 「图片生成」节点改为浅色扁平占位样式（居中图标、标题与提示、虚线细边框），点击仍打开内置生图面板，尺寸与行为不变。
+ [调整] 「图片生成」节点改为单一面板：移除节点占位卡片与下方独立浮层，模型、尺寸、张数、参数矩阵与变量入口以及「生成」按钮全部内联在节点内，提示词与参考图通过连线输入，生成结果按指定张数添加到画布。
+ [调整] 提示词节点改为单一可编辑面板：移除下方独立浮层，节点内直接显示标题、预设库入口与可直接书写的提示词文本域，右侧保留连线出口，可与生成节点连成一条流程。
+ [调整] 画布节点统一为无边框纯色底：移除所有节点卡片的默认描边（仅保留选中、关联、拖到画板等状态边框），智能画板的虚线轮廓改用与整体一致的细线色。
+ [调整] 重做「图片生成」节点配色与排布：模型与尺寸控件改为扁平融入节点底色（去掉灰色描边胶囊），次要操作（参数矩阵、变量、同种子重播）与「生成」按钮合并为贴底一行、按钮改为右侧紧凑圆形主色按钮，默认高度由 240 收紧到 180，输入摘要改用对比更高的次要文字色。
+ [调整] 左侧面板新增「画布」页签，与「素材」同级：画布页签只列出当前分组内的画布（含节点数、当前画布高亮），支持按标题搜索，并可直接在当前分组内新建画布并进入，不再占用素材页签顶部。
+ [调整] 本地文件夹（资源库）从设置中移出，改为放在左侧「素材」页签顶部：可直接绑定/更换文件夹、立即同步、解除绑定，并显示同步状态与上次同步结果；设置里不再有「工作区」页签。
+ [优化] 优化画布缩放流畅度：滚轮缩放改为每帧最多提交一次视口更新（与平移一致，不再每个滚轮事件都整页重渲染）；缩放进行期间临时关闭节点与画板的背景毛玻璃（停止缩放约 160ms 后自动恢复），避免缩放时大量模糊层被反复重新合成。
+ [优化] 图片生成节点的「生成」按钮改用画布暖色主题（浅色 `#57534e` / 深色 `#d6d3d1`），不再使用应用主色的近黑填充，避免在暖灰玻璃卡上突兀；同时收紧为紧凑圆角按钮并统一 hover 反馈。
+ [调整] 图片生成节点把生图参数全部内联展开：质量、宽高尺寸（含 16 倍数对齐）、分辨率、宽高比、透明背景、生成张数都直接显示在节点里，去掉原来的「Auto · 1:1 · 1 images」折叠胶囊，节点默认尺寸随之调整为 340×560；宽高比在节点内使用紧凑胶囊排列；同时修复图片模式误显示文本推理设置的问题。
+ [调整] 图片生成节点改为固定比例设计稿并等比缩放：节点按 340×560 的设计尺寸排版，调整大小时锁定宽高比，节点内 UI 整体按比例放大或缩小（保持控件与节点的占比不变），不再因改尺寸而重新换行。
+ [调整] 重做节点右下角的缩放手柄：弧线由 20px / 3px 边框增大到 32px / 6px 边框，命中区域由 32px 扩大到 48px，并向外平移约 18px（原先约 6px），同时改用更清晰的次要色，使手柄更容易被发现和拖动。
+ [调整] 图片生成节点的「生成」按钮改用淡可可色：新增画布主题的 `primary` / `primaryText` 颜色（浅色 `#e0cbb0` 底配 `#4a3a2a` 字，深色 `#c9ac8b` 底配 `#2a2118` 字），替换此前的深灰填充，使主按钮与整体淡可可配色一致。
+ [调整] 生成出来的图片不再与生成节点连线：结果图片按生成位置直接排布在画布上，画布连线只保留用户手动创建的连接，避免生成一次就多一条线。
+ [新增] 生成图片的出现动效：结果图从 loading 变为 success 时会播放一次轻微放大 + 上浮 + 淡入的入场动画（约 0.42s），已存在于画布上的图片不会重复播放，并遵循系统的「减少动态效果」设置。
+ [调整] 智能画布工具栏去掉「预览合成」按钮，改为**双击画板**直接打开放大预览，工具栏更精简。
+ [新增] 智能画布工具栏新增「存为图片节点」按钮：一键合成当前画板并直接生成图片节点放到画板右侧，不必先进预览弹窗再点按钮。
+ [调整] 图片生成节点的悬停工具栏精简：去掉「信息」「重试」「图层」三个动作（图层因为该节点固定在最底层而失去意义）。
+ [调整] 图片生成节点与智能画布一致，始终位于画布最底层（不再因选中而浮到最上方），避免遮挡其他节点。
+ [调整] 生成结果统一不再自动连线：视频、音频、文本的生成结果与图片一致，直接排布在画布上，不再自动连回生成节点；同时移除 `insertDerivedAsset` 中自动创建来源连线的 `relation: "generation"` 传参（原先它会额外建一条线）。智能画板的图层连线、mask 编辑的来源连线保持不变。
+ [修复] 图片生成节点改为固定比例设计稿并等比缩放后内容显示不全：设计稿由 340×560 调整为 412×608（四个模式选项原本超出右边缘约 61px、底部参数区被裁掉约 39px），节点默认尺寸同步为 412×608，内容不再被截断；旧节点仍按等比缩放完整显示。
+ [修复] 画布顺序不再随切换变动：画布切换栏原先按「最近更新」排序，切换画布会刷新 updatedAt 导致列表顺序跳动；现改为沿用画布数据里的固定顺序（随画布数据一起全局持久化），新建画布仍排在最前。
+ [调整] 节点名称默认隐藏：**不再提供全局「显示全部名称」开关**（按要求已移除），改为在节点悬停工具栏上按单节点切换是否显示自己的名称，选择随画布一起保存。
+ [新增] 画布列表支持拖拽重排：左侧切换栏里可直接拖动画布调整顺序，拖动时有插入指示线与被拖行淡化反馈，顺序写入画布数据并全局生效；重排不会误触发画布跳转。
+ [调整] 输入节点与输出节点合并为单一「资源」节点：底部工具栏与右键创建菜单只保留一个「资源」节点，可绑定本地文件夹、查看并拖出文件，同时保留「绑定输出文件夹并自动写入生成结果」的能力；两个文件夹 store 合并为一个，移除旧的 Output 节点类型与相关死代码。
+ [新增] 图片收进「资源」节点文件夹：把图片节点拖到资源节点上松手，原节点会动画回弹到原位（画布布局不变），图片被写入已绑定的文件夹，并显示「保存中 → 成功/失败」状态，文件列表自动刷新；拖到资源节点上方时该节点会高亮提示。资源文件夹改为读写授权，用户拒绝写入权限时显示失败态而不是报错。
+ [调整] AI 设置精简为只填 OpenRouter API Key：移除渠道列表、渠道名称、Base URL、模型能力选择与模型脚本编辑器等配置入口，Base URL 固定指向 OpenRouter；图片生成模型写死为 `openai/gpt-image-2.5-sunburst`。未配置 Key 时仍然禁止生成。
+ [调整] 主页与画板配色统一：应用级主题（画布列表页、设置弹窗、下拉与输入控件、浮层）改为与画布同一套暖色 token，深浅色都同步，主页不再是一片惨白。
+ [修复] 画布记住每张画布的缩放与镜头位置：根因是「容器尺寸测量」的 effect 在首次进入时容器还没挂载（骨架屏阶段），等画布恢复完才第一次真正执行，把刚恢复的 viewport 覆盖成居中默认值、并把这个错值写回画布存储，于是每次打开/切换/刷新都回到原点。现在测量只管测量、不再改写 viewport，各画布分别记住自己的镜头。
+ [调整] 提示词节点的参考图芯片从节点外面移到节点内部、输入栏正上方，拖动芯片到编辑器插入提及的行为保持不变。
+ [修复] 主页顶栏下方的分割线不再断开：分割线改由顶栏容器本身绘制、横跨整条 bar 到边缘，激活的菜单项不再覆盖它（指示方式改为不遮挡分割线），与左侧栏竖线的交界也接成干净直角。
+ [调整] 主页画布库改为全宽表格：去掉圆角矩形卡片容器，画布以表格行展示（保留名称、节点数、连接数、更新时间），并支持拖拽行来调整画布顺序；与画布内左栏列表共用同一套全局顺序。
+ [修复] 提示词节点拖不进图片生成节点的插槽：根因是提示词节点的编辑器容器吞掉了 `mousedown`（`stopPropagation`），导致**从节点主体按下时根本不会开始拖动**（只有顶部很窄的一条能拖），所以插槽看起来「放不进去」。现已修复，并新增 `lib/canvas/canvas-drop-bindings.ts` 统一「谁能放到谁身上、产生什么绑定」的判定，配套补上 e2e 断言防止再次静默回归。
+ [修复] 图片生成节点底部「生成」按钮贴着底边：重排设计稿内间距，按钮四周（尤其下方）恢复留白，内容不再被挤到边缘。
+ [调整] 删除「参数矩阵」与「提示词变量」两个功能：面板入口、生成代码路径、库函数、相关弹窗与双语 i18n 键全部移除（含对应 e2e 断言）。
+ [调整] 撤销「插槽绑定」回到直接连线：提示词节点重新通过普通连线进入图片生成节点，删除节点底部插槽 UI、拖入绑定路径、`metadata.promptNodeId` 字段，以及未绑定时显示的红/绿指示灯（只保留 `blocked` 色值供账户面板与错误色使用）；生成输入重新从连线解析。参考图挂在提示词节点、连线「参考图 N」标签与提示词节点内的可拖芯片保持不变。
+ [新增] 单次调用显示真实费用：生成完成后从 OpenRouter 响应读取 `usage.cost`，缺失时用响应头 `X-Generation-Id` 调 `/generation?id=` 求证，再退回本地价目表估算；金额显示在图片节点信息条「名字 · 尺寸 · 大小」之后，并标注该数字来自真实返回还是本地估算。
+ [新增] 右上角 API Key 用量入口：设置按钮旁新增头像式入口，点开可查看**当前 API Key** 的总用量与今日/每周/每月用量；若该 Key 设过额度上限则同时显示剩余额度，未设置则明确提示「未设上限」。打开面板时与每次生成完成后自动刷新，并支持手动刷新。+ [调整] 图片生成节点改为插槽绑定提示词：节点底部新增插槽，把提示词节点拖到该节点上即完成绑定（松手后提示词节点回弹原位、画布布局不变），并新增红/绿状态灯——未绑定提示词时亮红灯并禁用生成，绑定后亮绿灯；图片生成节点不再接受任何接入连线，生成所需的提示词改从绑定的提示词节点读取。
+ [调整] 参考图改为挂在提示词节点上：参考图不再连到图片生成节点，而是连到提示词节点；每条「参考图 → 提示词」连线显示「参考图 N」标签，提示词节点上方为每个参考图显示可拖动的名字芯片，把芯片拖进提示词编辑器即在当前光标处插入对应提及，生成时使用这些参考图。
+ [调整] 画布左侧面板移到顶栏下方：顶栏改为横跨整个画布宽度、开关左侧面板时不再移动；打开左侧面板只挤压画布宽度，并在开合后重新测量画布尺寸保证点阵与标尺对齐。
+ [修复] 切换画布时整屏闪烁：骨架屏原先在每次切换都会替换整个页面（导致全屏闪一下），现在只在首次进入画布时使用；切换画布时只清空并重新载入画布内容，顶栏与左侧面板保持不动。
+ [调整] 画布切换栏去掉「新建画布」按钮，只保留搜索与画布列表；重做分组名称的显示，不再直接拼出 `group:xxx` 这种前缀文案，改为带文件夹图标的独立分组标题行（与列表分隔）。
+ [调整] 重排图片生成/生成配置节点的面板：输入统计由一排带框胶囊改为一行扁平摘要（只显示非零项，无输入时给提示），参数矩阵、变量、同种子重播等改为扁平文字按钮，控件行整体收紧，生成按钮固定贴底；同时去掉图片生成节点内与节点标题重复的那行标题。
+ [调整] 画布内容节点（文本、提示词、生成配置、图片生成、输出）统一改用面板底色：浅色主题下为近白面板，深色主题下保持比画布更亮，并复用原有细边框，避免这些节点在画布上看起来是一坨暗块。
+ [调整] 统一画布库、提示词库、我的资产、设置页的边框、表面、圆角与悬停反馈，改用画布主题的 1px 细边框与面板色。
+ [调整] 应用导航、画布顶栏、底部工具栏、悬停/选择浮层、画布侧栏、小地图统一为毛玻璃表面：半透明底色、背景模糊与 1px 细边框，画布顶栏恢复为覆盖画布的通栏条。
+ [调整] 节点「层级」弹层改为紧凑数字控制：显示「第 N / M 层」与「上移一层 / 下移一层」两个按钮，不再列出全部节点。
+ [修复] 修复图片分辨率弹窗「算法」选项文字被截断的问题，改为三行可选列表完整显示算法名称与说明。
+ [修复] 修复拖动节点时连线不跟随的问题：拖动过程中连线端点改用与节点相同的位移预览坐标，连线实时跟随节点移动，松开后与落点保持一致。
+ [修复] 修复连线上的关系标签无法点选的问题：点击连线中点标签现在与点击连线本体一样选中该连线，且不会被当作点击空白区域。
+ [修复] 修复画布标尺：横向标尺原先被通栏顶栏整条盖住而完全看不到，现改为两条标尺都渲染在顶栏下方并拼成 L 形，竖向标尺上下都贴住画布区边缘；同时修复画布尺寸测量在项目加载前取不到容器、导致标尺（与小地图）一直使用默认尺寸的问题。
+ [修复] 修复从节点面板打开的弹层无法交互的问题：预设库、参数矩阵、变量、模型与尺寸选择等弹窗弹出后，画布会把弹窗内的按下当成「点击空白」并抢占指针（setPointerCapture），导致后续点击全部被重定向到画布、弹窗点不动且节点跟着鼠标移动；现在画布容器与节点卡片的指针处理统一识别弹层区域并跳过（提取共享判定，避免多处守卫不一致）。
+ [新增] 图片节点新增「分辨率」工具：弹窗提供「算法放大」「算法缩小」「AI 提升」三个页签，可本地按算法放大或缩小图片，也可调用已配置的图片模型提升分辨率，并在右侧生成结果节点。
+ [调整] 移除图片节点未实现的「超分」入口，原「放大」工具更名为「分辨率」。
+ [调整] 画布连线关系标签放大并保证每条连线都有标签（无法识别来源时显示「关联」），图片节点信息固定显示在左上角并按「名称 · 尺寸 · 大小」排列，选中与连线描边改为柔和灰色，图片节点移除「复制提示词」「反推提示词」「替换图片」「存资产」四个工具栏操作。
+ [新增] 画布连线在中点显示关系标签（如「参考图」「抠图结果」），并把关系记录在连线上，方便后续编辑和区分不同来源。
+ [新增] 图片节点悬停工具栏新增「抠图」，调用浏览器端背景移除模型生成透明底图片并在右侧创建、选中结果节点（首次使用需联网下载约 40MB 模型，之后使用缓存）。
+ [调整] 抠图模型改为按需下载：背景移除库懒加载，页面不再打包模型资源，可在 设置 → 本地模型 下载，首次使用抠图时也会自动下载并显示进度。
+ [新增] 画布新增「智能画布」节点：固定比例与合成分辨率的画板，可把图片节点拖到画板上按位置摆放（图片仍为普通可编辑节点），一键把画板范围内的图片合成一张图并在大图弹窗预览、下载，合成结果可作为下游生成节点的参考图输出。
+ [调整] 设置「偏好设置」页签集中展示界面语言、键盘快捷键和版本信息，顶栏右上角只保留设置与主题切换。
+ [调整] 移除画布顶栏与顶部导航的 Agent 聊天/连接入口，Agent 仅保留后台 MCP/SSE 桥接控制画布。
+ [优化] 优化手机、平板和桌面下的自适应布局：画布库、提示词库、资产页与设置页的标题栏和栅格按宽度换行分列，画布侧栏在手机上改为覆盖式浮层并新增收起按钮，画布底栏与缩放控件在窄屏不再重叠，固定宽度弹窗适配手机宽度。
+ [调整] 主题切换移入设置「偏好设置」，顶栏右上角只保留设置按钮。
+ [调整] 渠道配置默认留空，不再预置默认渠道与模型；允许删除全部渠道并显示空状态提示。
+ [调整] 设置标题简化为「配置」，移除配置的导入/导出功能，并整理设置弹窗布局。
+ [调整] 全局字体改为圆体：英文 Baloo 2、简体中文资源圆体（Resource Han Rounded SC，含 Regular 与 Bold 两个字重）。
+ [调整] 渠道固定为 OpenRouter：设置中仅填写 OpenRouter API Key，Base URL 固定为 `https://openrouter.ai/api/v1` 不可修改；生图改用 OpenRouter 专用 `/images` 接口（图片以 `b64_json` 返回并按 `media_type` 还原，图生图通过 `input_references` 提交参考图），移除 Gemini 协议及相关分支，模型列表从 OpenRouter 拉取并按输出模态自动归类能力。
+ [调整] 画布 MCP 融合进前端：删除独立 `canvas-agent` / `canvas-mcp` 包，浏览器 SSE 桥接与 `/mcp` HTTP MCP 端点改由前端 Vite 服务在同一端口 `3000` 上提供（`http://127.0.0.1:3000/mcp`）；网页同源自动连接、不再需要 token 或配置文件，并移除 `plugins/infinite-canvas` Codex 插件。
+ [调整] 首页改为直接进入画布库；画布库改为左侧库列表、右侧单行画布列表，库为普通文件夹（不再有「全部」「未分组」），支持新建、重命名、删除库以及把画布移动到其他库，删除库会连同库内画布一起删除；同时修复画布行标题与统计上下堆叠的问题，并统一画布库页面的间距与操作区样式。
+ [调整] 画布底部工具栏改为在画布区域内真正居中；左下角独立的缩放控件收进工具栏，新增「缩放」工具，弹层内提供缩放滑杆与百分比、重置视图、小地图开关和快捷键入口。
+ [调整] 配置移除本地代理功能（含独立的 `canvas-proxy` 转发工具），「偏好设置」拆分为外观、默认模型、生成、关于四个页签，并移除配置页说明文字与弹窗底部「完成」按钮。
+ [调整] 外观设置的主题选项改为「白 / 黑」且默认使用白色主题，默认界面语言改为英文（已选择过的语言仍然优先），移除语言说明文字，默认生图张数改为 1；外观新增「画布背景」选项（点 / 线 / 空白，默认点），新建画布默认使用该背景。
+ [调整] 画布外观设置（主题、画布背景、图片信息）从画布工具栏弹层移入设置「外观」，画布工具栏不再提供外观按钮，画布外观统一跟随设置。
+ [调整] 画布左侧面板移除「画布元素」页签，只保留「资产」和「提示词库」，默认显示「资产」。
+ [调整] 画布「资产」页签支持手动分组（可折叠，未分组资产归入「未分类」，资产可在分组间移动），并支持把资产直接拖到画布上，在放置位置生成对应节点。
+ [调整] 画布内的节点、选区/悬停工具栏、连线、弹层、菜单、小地图、底部工具栏和资产卡片统一移除投影/阴影，仅保留描边、轮廓环和边框高亮。
+ [调整] 画布底栏移除「上传资产」和「生成配置」两个按钮，保留移动、撤销/重做、文本/图片/视频/音频/组节点、画布外观、缩放与清空。
+ [调整] 画布移除空白画布创建菜单与节点、连线右键菜单，改为使用浏览器原生右键菜单；视频首尾帧截取与复制节点移入节点悬停工具栏。
+ [调整] 图片节点改为纯展示与参考用途，点击不再打开底部生成面板，连接图片到生成节点时仍可作为参考图。
+ [新增] 画布新增「图片生成」内置节点，仅用于生成图片，复用生成配置的生图流程，可在底部工具栏添加。
+ [新增] 智能画布支持画板背景色（透明 / 白 / 黑 / 自定义，并同步到合成图），新增「一键排版」网格整理与把合成图「存为图片节点」。
+ [修复] 智能画布画板内的网格参考线未能渲染（背景色简写覆盖了背景图样式），现已正常显示。
+ [修复] 智能画布画板会盖住板内图片导致无法选中，画板改为底层渲染，板内图片可正常点击和拖动。
+ [新增] 置入画板的图片显示「已置入画板」角标与虚线描边提示，拖动画板时板内图片一起跟随移动并保留归属。
+ [新增] 画布支持调整图片节点的前后叠放层级（悬停工具条「上移一层 / 下移一层」），拖动节点时显示与其他节点左中右、上中下对齐的吸附参考线。
+ [新增] 画布 MCP/Agent 支持控制智能画布：可创建 `smart-canvas` 与 `image-generation` 节点，用 `place_on_board` 置入或移出图片、用 `arrange_board` 一键网格排版。
+ [新增] 智能画布支持文字标注：可在画板上添加文字、拖动位置、双击编辑、悬停删除，标注会按位置与样式合成进导出的图片。
+ [调整] 顶栏改为通栏布局，左上角 logo 与页面内容左缘对齐（此前受 1280px 居中容器限制而内缩）。
+ [修复] 下载或运行抠图模型时会阻塞主线程导致整个界面卡死，现改为在 Web Worker 中执行，模型下载、onnxruntime 初始化与推理期间界面保持可操作。
+ [调整] 设置「生成」页签只保留画布默认生图张数（默认 1 张），移除音频声音/格式/语速设置，以及底部的音频指令与系统提示词输入框。
+ [调整] 设置「模型」页签暂时只保留默认图片模型，视频 / 文本 / 音频模型待后续开发。
+ [优化] 修复遮罩编辑弹窗画笔卡顿：画笔圆环改为直接更新 DOM 且按帧合并指针移动，绘制过程不再触发弹窗整体重渲染，并缓存画布 2D 上下文。
+ [调整] 图片节点悬停工具栏合并「上移一层 / 下移一层」为「层级」弹层：按渲染顺序列出节点层级并高亮当前节点，可逐层上移或下移；智能画布不参与分层。
+ [调整] 图片节点「配置快捷工具」改为「显示文字」开关，图片信息移到图片上方并新增节点信息按钮，查看大图按钮移到图片右下角。
+ [调整] 画布顶栏改为半透明浮层，移除三横线菜单，只保留返回画布库、画布标题与设置。
+ [调整] 设置弹窗改为固定尺寸的纯色面板：左侧纵向页签、固定内容高度与内部滚动，切换页签不再改变弹窗外框；弹窗与抽屉恢复不透明背景与 1px 细边框，毛玻璃只保留在画布组件表面。
+ [调整] 设置「渠道」页签改为扁平行列表：渠道名称与模型数量、接口地址分行显示，编辑/删除改为紧凑图标按钮，并补充空状态与新增渠道入口。
+ [新增] 设置「本地模型」新增「删除」：清除下载状态并清理浏览器缓存的模型文件；「本地存储」新增本地模型占用行，点击可跳转到本地模型页签。
+ [调整] 设置「关于」页签改为扁平快捷键列表与版本区块，快捷键按键样式在浅色/深色主题下统一。
+ [调整] 移除设置中的 WebDAV 同步页签，并删除相关的同步服务、配置字段与文案。
+ [新增] 设置新增「成本」页签：按 USD 显示总花费、记录总数与未定价条数，按模型拆分生成次数与花费，并列出最近生成记录的时间、模型、数量与成本，未定价模型明确标注为「未定价」且不计入总花费，支持一键清空记录。
+ [调整] 画布节点卡片与智能画布改用与工具栏、浮层一致的半透明毛玻璃表面（同一底色、背景模糊与细边框），节点内加载与占位块不再铺实心灰底，图片/视频等媒体节点保持透明，节点设置入口、连接创建菜单与侧栏资产卡片改为扁平无底色。
+ [调整] 移除画布「框架」与「组」节点及其组合/取消组合能力：删除底部工具栏的框架按钮、选择浮层的组合与取消组合操作、`Ctrl/Cmd+G` 与 `Ctrl/Cmd+Shift+G` 快捷键，以及 MCP 的 `canvas_group_nodes` / `canvas_ungroup_nodes` 两个工具，画布节点类型只保留图片、文本、提示词、生成配置、图片生成、视频、音频、智能画布与输出。
+ [调整] 撤销提示词→图片生成节点的「插槽绑定」，回到直接连线：删除节点底部插槽、拖入绑定路径、`metadata.promptNodeId` 字段以及未绑定时的红/绿指示灯（`blocked` 色值保留给账户面板与错误色复用）。
+ [修复] 图片生成节点的生成按钮不再因为「没有连入提示词」而被禁用：禁用态在浅色卡片上几乎不可见，看起来就像按钮消失了。
+ [调整] 节点上方名称改为始终显示；工具栏里的显示/隐藏开关改为控制「工具栏按钮后面的文字标签」（对所有节点类型生效并持久化），不再影响节点名称。
+ [调整] 节点删除操作移到悬停工具栏最末位并改为红色（复用画布已有的语义红，不硬编码色值）。
+ [修复] 图片生成节点面板：按宽高中较小者等比缩放，彻底避免任何尺寸下内容被裁切；内容在节点内上下左右居中；设置行不再被挤窄；生成按钮紧跟「图片数量」、占满宽度并改为透明底描边样式；移除底部多余空档。
+ [调整] 底部工具栏移除独立的新增图片入口（图片改为从「资源」节点拖出），节点创建项顺序调整为 资源 → 提示词 → 生成 → 智能画布。
+ [新增] 智能画布图层支持全部 Photoshop 混合模式与不透明度，并支持配置背景颜色与背景不透明度。
+ [修复] 智能画板上放置的图片按图层真实位置与尺寸绘制（此前与图片节点的位置不一致）；画板不再实时渲染合成结果，改为双击预览、存为图片节点或下载时按需合成。
+ [调整] 智能画板工具栏移除「预览合成」（改为双击打开），「存为图片节点」提升为工具栏工具，预览弹窗只保留底部一个黑色下载按钮。
+ [调整] 图片的下载入口从节点左上角与悬停工具栏移入双击预览的底部黑色下载按钮（视频、音频的下载入口保持不变）。
+ [调整] 派生生成的图片不再与来源节点连线；图片节点不再接受任何连入（校验层拒绝、也不渲染接入点），图片作为连线源（参考图）不受影响。
+ [新增] 右键接管与首个右键工具：全局压掉浏览器原生右键菜单并提供统一的右键工具扩展点，`Ctrl+右键拖拽` 绘制笔迹，被笔迹穿过的连线会被切断且可撤销。
+ [调整] 彻底移除提示词库体系：删除 `/prompts` 页面与顶部导航入口、「设置 → 提示词来源」页签、画布提示词面板的「从预设库选择」入口及配套 store / service / hook，移除 MCP `prompts_search` 工具（画布工具从 33 个减为 32 个）；提示词节点的编辑、标签与引用能力保持不变。
+ [调整] README 重新对齐当前实现：删除「文档 / 社区支持 / 开源协议」章节，内置节点数从 17 更新为 18 并补上「音频合成器」，新增「工作区 / 图像工作台 / 音频合成器」说明，修正底部工具栏创建分组顺序以及资源节点、智能画布、画布拖拽等描述。
+ [调整] 画布浏览器桥与 MCP 服务从前端内置改为独立的本地 Python 服务（默认 127.0.0.1:3210，协议版本 6 与 32 个工具不变）：前端不再随 Vite 启动桥接，改为通过可配置的服务地址与 Bearer 令牌连接，设置新增「本地 Agent 连接」可填写地址与令牌（保存在浏览器本地、保存后自动重连并显示连接状态）。
+ [新增] 画布新增「故事图」写作体系：新增 story 节点（前提 / 幕 / 章 / 场景 / 节拍 / 情节点 / 人物 / 地点 / 传说 / 线索），底部工具栏新增一级「文字」菜单集中创建这十种节点；故事节点之间以及设定节点与故事节点之间按两端关系自动生成带类型的连线（故事流 / 角色弧光 / 场景设定 / 主题 / 线索）并显示在连线中点；故事节点可「生成」正文，生成时会自动读取相连的人物、地点、传说、线索与最多 3 个前序节点（入边故事流）的正文并追加写回本节点；可「展开」一级结构（前提→幕→章→场景→节拍）自动创建子节点与连线，配合节点悬停工具栏的「整理」按左右树形重新排布；MCP 新增 `canvas_expand_story` 工具（画布工具 32 → 33）。
+ [调整] 新建节点、复制/粘贴节点以及单击节点不再自动在节点下方弹出生成面板；该面板改为只能从节点列表的「面板」按钮或插件调用显式打开。
+ [新增] 设置新增「服务商」与「模型」两个页签：「服务商」可添加多个服务商，每个服务商有独立的 Base URL 与 API Key，内置 OpenRouter / OpenAI / Groq / DeepSeek / Gemini / Moonshot / 智谱 / SiliconFlow / Cohere / Ollama 预设并可一键拉取该服务商的模型列表；「模型」按 文字 / 图像 / 声音 / 语音 / 视频 五个分类汇总所有服务商的模型、可改分类、可设每个分类的默认模型。模型下拉框统一显示 `服务商/模型`，请求按所选模型路由到对应服务商自己的地址与密钥（此前写死 OpenRouter 与单个全局密钥），语音（TTS）从音频中拆分为独立分类。
+ [调整] 设置「服务商」页签改为可折叠列表：每个服务商折叠展示名称与模型数量，展开后编辑预设 / 名称 / Base URL / API Key；删除改为两次点击确认（首次点击变为确认态、再次点击才删除，失焦即取消）。

+ [新增] MCP 从「只服务画布」升级为通用的「页面 + 动作命名空间」中继：`app_get_state` / `app_describe_actions` / `app_apply_ops` 三个通用工具按「列出 → 描述 → 调用」驱动任意页面，Agent 协议版本从 6 升到 7。
+ [新增] 页面动作命名空间落地三个：`canvas` 扩展到 49 个操作（节点层级 / 锁定隐藏 / 批量重命名 / 字号 / 自由缩放 / 排版 / 视频槽位 / 画板与异步处理等），新增 `image`（36 个操作，图像工作台图层、文档、滤镜与导出）与 `audio`（65 个操作，轨道、MIDI、混音、走带、自动化与导出）。
+ [新增] 新增 `config` 命名空间（12 个操作）读写服务商、模型与 API Key，默认标记为 `danger` 且默认拒绝：快照始终把密钥脱敏为 `{ hasKey, last4 }`，写入密钥与读取原始密钥需分别显式授权。
+ [删除] 移除 `canvas_get_state` / `canvas_get_selection` / `canvas_export_snapshot` / `canvas_apply_ops` 四个画布工具，MCP 工具总数从 30 变为 29（3 个通用页面工具 + 3 个站点工具 + 23 个画布快捷宏工具）。

+ [调整] 音频合成器的媒体库改为拖拽入轨：删除每行的「+」和「添加到所选轨道」的点击流程（不再需要先选中音轨），直接把素材拖到某条音轨的时间位置即可在该处生成片段并按网格吸附；音轨头部「添加片段」的加号一并移除（MIDI 轨「添加区域」的加号与底部「新建轨道」保留）。
+ [调整] 音频合成器移除顶部菜单栏的「轨道」菜单，功能重新分配：五种新建轨道（音频 / 乐器 / MIDI / 编组 / 返回）收进「添加轨道」下拉菜单，轨道级的「复制轨道 / 删除轨道 / 导出分轨」移到每条音轨行右侧新增的「⋯」按钮（原右键菜单同样保留）；菜单栏只剩编辑 / 片段 / 视图 / 自动化四项。
+ [新增] VST3 音源：安装本地 VST 宿主后，可在钢琴卷帘的「音色」下拉里把已安装的 VST3 乐器设为音轨音源（列表来自宿主扫描并按「名称 · 厂商」展示）；宿主目前需手动启动，未运行时下拉只保留内置音源并显示「本地 VST 宿主未运行」，不会有报错或卡顿。

## v0.18.0 - 2026-09-07

+ [调整] 图片 1K / 2K / 4K 按宽高比使用固定尺寸表。
+ [调整] 开启本地代理后，WebDAV 测试连接和同步也经本机代理转发，关闭后仍直连。
+ [修复] WebDAV 同步会记录已删除画布，再次同步时不再把远端旧画布恢复到本地。
+ [新增] 配置弹窗新增「本地代理」页签，开启后模型列表、生图、生视频、生成文本、生成音频等请求经本机代理转发，配套提供纯转发工具。
+ [新增] 多选节点后显示整体虚线选区，并可一键打组，用组节点包住选中内容，支持解散组。
+ [调整] 图像设置将 1k/2k/4k/auto 与宽高比拆开并据此计算尺寸；视频设置用 480p/720p/1080p 与 1:1、3:4、4:3、16:9、9:16、21:9 计算宽高，时长改为 4–30 秒滑杆。
+ [调整] 无自定义脚本时，OpenAI / Gemini 视频内置请求与插入模板使用相同字段，按模式发送首尾帧或全能参考。
+ [新增] 视频设置支持首尾帧和全能参考模式，调用脚本可通过 params.mode 区分参考图字段。
+ [修复] Gemini 生图改为使用 generationConfig.imageConfig 传递比例和尺寸，不再使用 responseFormat。
+ [修复] 调用脚本默认模板改为英文函数写法，并用 JSDoc 标明全部字段，同时补齐图片质量、背景和视频参考素材。
+ [优化] 视频模型自定义调用脚本可使用参考视频和参考音频；调用脚本改为全屏分步配置，并支持复制写脚本说明给外部 AI。
+ [新增] 画布视频节点会保存远端任务 ID，刷新后自动继续查询状态，也可手动获取任务状态，直到成功、任务失败或查询报错。
+ [修复] 我的资产下载图片改为读取本地文件内容，不再因预览地址和下载地址不一致或标题含中文、空格、书名号而失败。
+ [修复] 生成提示词附带的上游文本改为按「文本N」编号分块，避免多段文本只靠空行分隔而无法与提示词中的引用对应。
+ [修复] 多参考图编辑改为按 OpenAI 规范使用 `image[]` 提交多张参考图，避免兼容接口因重复 `image` 字段拒绝请求。
+ [修复] 通过 URL 导入 API 凭据时按 Base URL 更新已有渠道或新增独立渠道，不再覆盖渠道列表第一项；缺少或非法 Base URL 时不修改配置。

## v0.17.0 - 2026-09-02

+ [修复] 文档站默认英文路径不再因内部语言重写产生重定向循环。
+ [优化] 文档站移动端折叠菜单新增分类切换入口，桌面端增加随滚动高亮的本页目录。
+ [优化] 画布左侧元素列表按组展示树形层级，组内节点支持展开和收起。
+ [优化] Canvas Agent 生成流程复用仅由现有节点引用构成的提示词，避免创建重复文本节点。
+ [新增] 节点输入框上方新增参考内容栏，可预览、移除或从画布连续选择参考节点；组引用会展开组内全部内容，移除任一项即断开整组。
+ [新增] 视频节点右键菜单支持截取首帧、尾帧和当前帧并生成图片节点，方便衔接连续镜头。
+ [调整] 移除文本节点右上角的生图按钮，让正文区域保持简洁完整。
+ [调整] 多次文本生成合并为单个文本节点，其他结果作为可展开切换的备选文本。
+ [新增] 组节点可作为生成输入并通过单条连线或 `@` 一次性引用组内全部有效资源。
+ [修复] 透明图片在多图备选展开时保持透明背景，不再显示白色卡片底色。
+ [修复] Canvas Agent 转发 CLI 错误日志前脱敏常见凭据，避免调试日志和浏览器事件流泄露密钥。
+ [修复] Canvas Agent 配置目录和配置文件收紧访问权限，降低本地连接 Token 被其他系统用户读取的风险。
+ [修复] Canvas Agent 自动连接凭据改用 URL fragment 传递并在读取后立即清除，避免 Token 进入服务器日志、Referer 和浏览器历史。
+ [修复] GPT Image 模型请求不再发送不受支持的 `response_format` 参数。
+ [修复] 图片接口返回临时外链时统一下载、校验并本地保存，支持取消下载，跨域无法读取时保留可显示的原链接。
+ [调整] 局部遮罩编辑改为在画布生成一张遮罩标注图，不再使用接口的 mask 参数；

## v0.16.0 - 2026-08-18

+ [新增] 提示词来源新增 Freestylefly GPT Image 2 内置来源。
+ [调整] 移除火山方舟协议及 Seedance 专用适配。
+ [调整] 画布默认使用移动工具，可直接拖动元素或空白画布，按住 Control 可临时框选节点。
+ [调整] 双击多图节点统一放大预览图片，仅通过右上角张数按钮展开或收起图片组。
+ [修复] 文本节点和生成配置新增独立的文本生成次数，默认一次且不再误用生图数量。
+ [修复] 节点缩放期间暂停渲染提示词面板，避免导入图片后立即缩放导致页面崩溃。
+ [优化] 生图、视频和音频请求遇到跨域拦截时明确提示需要通过自己的服务转发。
+ [优化] 图片工具条默认仅显示图标并移除图片、文本编辑入口，单击图片、文本或视频节点直接打开下方提示词面板。
+ [优化] 画布多图展开状态改为由用户主动控制，操作画布其他区域时不再自动收起。
+ [优化] 展开的多图子图支持双击直接放大预览，无需先设为主图。

## v0.15.1 - 2026-08-07

+ [调整] 项目开源协议更换为MIT，允许所有人免费用于开源、闭源和商业场景。

## v0.15.0 - 2026-08-07

+ [新增] 画布节点提示词支持弹窗放大编辑和实时同步。
+ [新增] 配置页新增本地存储、站点配额与对象仓库占用信息。
+ [新增] 画布多图支持失败项重试与删除、创建副本和单图下载。
+ [新增] Agent 输入框支持通过 `/` 快速选择 Skill，并通过 `@` 将画布素材按光标位置插入正文，输入及发送后均支持悬浮预览。
+ [优化] 画布新增选择与移动模式，并支持 Control 或空格临时反转工具。
+ [优化] 画布多图合并为可展开的单节点，支持实时状态、主图切换和四列布局。
+ [优化] 从左侧元素列表定位节点时，自动缩放上限调整为 100%。
+ [优化] Agent 用户消息元数据改为独立的版本化文件存储，刷新或重启后仍可恢复附件、Skill、画布引用及本地缩略图，未知存储格式不会被覆盖。
+ [修复] 关闭输入面板后保留图片与文本节点的编辑提示词。
+ [修复] 点击画布空白处时同步收起节点工具条和输入面板。
+ [修复] 空格键切换工具时不再误触当前聚焦的按钮。
+ [修复] 清理本地图片时保留工作台生成历史引用的文件。
+ [修复] 修复画布多图切换主图、重试和调整尺寸时的缩放与状态异常。
+ [修复] 统一画布多图操作按钮在不同主题下的颜色与对比度。

## v0.14.0 - 2026-08-05

+ [新增] 文档站支持英文默认与简体中文切换，正文、导航、搜索及 LLM 文档接口按语言独立展示。
+ [新增] 前端搭建中英文国际化框架，支持从右上角一键持久化切换全局导航、首页、配置中心、Ant Design 和日期组件语言。
+ [修复] 统一 Ant Design 下拉菜单与选择器在浅色、深色主题下的弹层及交互状态颜色。
+ [修复] Agent 顶栏在英文模式或可用宽度不足时自适应隐藏标签文字，避免内容被裁切。
+ [新增] Agent 支持查看、创建、编辑、删除、启停和显式调用本地 Codex Skill，并同步多标签页变更。
+ [新增] Agent 支持从当前对话或当前画布生成可编辑的 Skill 草稿，并在用户确认后保存。
+ [调整] Agent 面板连接状态、内容标签和新对话统一排版，窄面板仅隐藏文字并保留图标与数量。
+ [优化] Agent 输入区控件随面板宽度在图标与图标加文字之间自适应。
+ [修复] Agent Skill 草稿期间保持 MCP 活动画布隔离，支持停止临时生成任务，并禁止并发修改 Skill。
+ [修复] Agent 新建或恢复对话时统一同步版本化会话与 MCP 初始化状态，避免多页面竞态误触 `409`、提前发送或任务后残留初始化卡片。
+ [优化] 提示词中心缩小卡片、每行展示四张，移除“加入我的资产”按钮边框。
+ [优化] 缩小生图与视频工作台的提示词库弹窗，精简卡片。

## v0.13.0 - 2026-08-03

+ [优化] Agent 进入空白对话或点击新对话后后台预热 Codex 与 MCP。
+ [优化] Agent 排查日志改为筛选栏结构化滚动列表，支持筛选、展开详情并折叠连续重复事件。
+ [优化] Agent 日志统一使用居中的回到底部入口，向上浏览时暂停跟随并可快速返回最新内容。
+ [优化] Agent 对话将同一轮连续命令合并为按数量折叠的命令组，默认隐藏冗长命令预览。
+ [修复] MCP 初始化期间禁用 Agent 按钮与回车发送，避免首条消息提前清除服务加载状态。
+ [修复] Agent 对话代码块关闭行号后多行内容被拼接为一行的问题。
+ [修复] 统一 Agent 实时事件与线程历史的消息归属，避免多窗口或刷新后出现过程重复等问题。
+ [修复] 节点缩放期间暂停渲染跟随节点的浮层工具条高频更新导致页面崩溃。

## v0.12.1 - 2026-07-31

+ [新增] 画布右侧Agent支持按当前账号可用范围选择Codex 模型与推理强度。
+ [优化] Canvas Agent 升级至最新 Codex，启动时检查版本更新，
+ [优化] 统一通过公共日志输出 Info 以上信息，美化转发日志时间。
+ [优化] Agent高速流式回复取消逐词动画排队和当前消息离屏占位。
+ [修复] Agent首次发送消息时立即保留并展示用户内容，不再晚于思考状态。

## v0.12.0 - 2026-07-30

+ [新增] 画布 Agent 支持三档 Codex 权限，并可在对话中审批操作。
+ [新增] Canvas Agent 支持 `--debug` 日志并按日期保存。
+ [新增] Agent 新增可折叠任务进度，实时显示各步骤处理状态。
+ [调整] Agent 工具确认模式移至输入框并默认自动确认。
+ [调整] 进入画布后默认开启空白对话，历史对话改为主动恢复。
+ [修复] Agent 默认通过节点生成，明确指定时才使用 Codex 内置生图。
+ [修复] Agent 本地路径改为在文件管理器定位，不再拼接 localhost。
+ [修复] Agent 启用 Codex 思考摘要，并修复历史恢复后过程记录缺失。
+ [修复] 修复 Agent 图片预览打开后产生额外空行的问题。
+ [修复] Canvas Agent 停止任务仅中断 turn，不终止服务进程。
+ [修复] Agent 修复运行提示闪退和回复需切换标签页才显示的问题。
+ [修复] Agent 任务失败时立即结束等待，并在对话和历史中显示中文原因。
+ [修复] Agent 修复图片附件偏移，历史恢复保留缩略图且不回显内部说明。
+ [修复] 修复画布复制快捷键拦截 Agent 对话和节点详情文本复制。
+ [优化] Agent「新对话」立即清空并后台同步，不再等待创建 Codex 线程。
+ [优化] Agent「读取画布」按节点类型和连线分类展示内容概览。
+ [优化] Agent 发送后立即清空输入框并展示消息，避免内容滞留或草稿被清空。
+ [优化] Agent 动态工具卡片显示名称、状态和失败原因，替代「工具操作」。
+ [优化] Agent 面板标题、功能标签和新对话操作合并为单行顶栏。
+ [优化] Agent Markdown 代码块与外链确认弹窗改为紧凑中文样式。
+ [优化] Agent 思考摘要支持 Markdown 渲染，改善列表和代码阅读。
+ [优化] Agent 思考摘要和命令改为无边框折叠行，保留实时返回内容。
+ [优化] Agent 用户图片改为紧凑缩略图并支持单击放大。
+ [优化] Canvas Agent 的 Codex CLI 升级至 0.145.0。
+ [优化] Canvas Agent Debug 日志改为纯文本单行格式。
+ [优化] 精简 Agent HTTP 诊断日志，过滤轮询、流式重复事件和原始响应。
+ [优化] Agent 对话移除头像，工具活动紧凑展示，Token 用量支持数字动画。
+ [优化] Canvas Agent 流式回复改为增量传输，减少长对话重复渲染。
+ [优化] Agent 新增过程时间线，工具卡片改为中文摘要和可读详情。
+ [优化] Agent 长时间等待时显示阶段、时长和停止提示，避免空会话读取失败。
+ [优化] Agent 历史记录支持多选批量删除，点击记录可直接进入对话。
+ [优化] Agent 优先操作当前画布，不再无故查询列表或重复导航。
+ [优化] Canvas Agent 独立维护工作区指令，不再重复拼接提示词。
+ [优化] Canvas Agent 目录按 Agent、画布、服务和工具拆分职责。

## v0.11.0 - 2026-07-28

+ [新增] 画布文本生成新增推理强度设置，支持默认调用与自定义调用脚本读取所选档位。
+ [新增] 生图与视频工作台的参考资产区域支持直接拖动文件上传。
+ [新增] 配置与用户偏好支持通过配置文件导入和导出。
+ [新增] 模型渠道新增火山方舟协议。
+ [优化] 画布左侧元素列表新增图片放大预览操作。
+ [优化] 图片遮罩、切图与裁剪编辑支持常用快捷操作并修复高分辨率图片预览闪烁。
+ [优化] 提示词详情弹窗改为上下布局并限制显示尺寸。
+ [修复] 画布组装提示词浮层限制正文高度，长提示词改为区域内滚动查看。
+ [修复] 修复画布节点提示词过长时无法使用鼠标滚轮滚动的问题。
+ [修复] 修复画布重试时组装提示词重复以及切换类型调用模型不一致的问题。
+ [修复] 修复生成完成后再次选中画布节点时提示词不再回显的问题。

## v0.10.0 - 2026-07-25

+ [新增] 提示词来源新增BananaPromptQuicker，并支持添加自定义标准 JSON 来源。
+ [新增] Agent 新增统一状态查询，支持查看画布、生图工作台和视频工作台任务进度。
+ [调整] 内置提示词来源改为读取ImagePrompts 统一JSON 数据，不再由画布端分别解析。
+ [调整] 提示词来源配置保留卡片式交互，并展示来源数量、同步状态和上次成功时间。
+ [优化] 提示词来源支持独立缓存和更新，更新失败时保留上一次成功内容。
+ [优化] Agent 对话区分用户与 AI 消息，并优化会话切换和回到最新消息的交互。
+ [优化] 画布生成完成后保留提示词输入，并改善提示词库和资产面板的交互性能。
+ [优化] 提示词中心标题居中、搜索输入增加防抖，并采用左侧筛选、右侧内容的双栏布局。
+ [修复] 本地 Agent 完善多标签页请求隔离、结果归属、焦点回退和 Codex 会话状态同步。
+ [修复] 本地 Agent 上传的图片附件可正确创建画布图片节点并连接生成流程。
+ [修复] 画布提示词库支持跨来源搜索，插入节点时保留提示词标题。

## v0.9.0 - 2026-07-17

+ [新增] 左侧面板「资产」Tab 支持上传添加图片/视频资产、卡片悬停移除资产。
+ [新增] 左侧画布面板支持拖拽调整宽度、展开/收起(带动画),顶栏菜单左侧新增面板开关按钮。
+ [新增] 顶栏菜单新增「导出当前画布」,导出为包含全部资源的压缩包。
+ [新增] 左侧画布元素列表支持多选并批量导出选中元素为压缩包。
+ [新增] 可选的网站统计分析:支持 Google Analytics 4 与百度统计。
+ [调整] 画布节点名称默认不再显示,仅在选中/悬停/编辑时出现,画布更简洁。
+ [优化] 左侧面板「画布/资产」切换改为带滑动下划线的动画,移除非图片元素图标的灰色底色。
+ [优化] 画布节点提示词面板 `@` 引用图片时,输入框内直接显示真实缩略图。
+ [优化] 移除画布节点右上角的「图片1/文本1」资源角标,引用改在对话面板 `@` 直接选取。
+ [修复] 连接本地 Codex Agent 后,拖拽画布节点边框缩放等高频编辑导致页面崩溃。

## v0.8.2 - 2026-07-16

+ [新增] 图像设置新增「透明背景」开关,开启后生成无背景的透明图像。
+ [优化] 画布节点输入区域移除灰色底色与边框、美化样式。
+ [修复] 画布节点提示词输入框补上悬停文本光标。

## v0.8.1 - 2026-07-16

+ [新增] 插件 SDK 扩展:AI 生成能力、面板控制能力。
+ [优化] 3D 全景(1.1.0)支持上传与 AI 生成并升级查看器;
+ [优化] HTML 节点(1.2.0)迁移到统一交互开关;
+ [优化] 便利贴(1.1.0)可拖动移动、自选颜色、移除资源角标与衍生功能;
+ [优化] SVG 节点(1.1.0)透明背景融入画布、可拖动、去除默认值;
+ [优化] 画布节点渲染性能:memo 化节点回调,交互时不再全量重渲染;
+ [修复] 修复 Markdown 节点在点击/移动视角时重复渲染导致图片反复请求的问题;
+ [修复] 修复插件版本号显示不更新，插件面板新增可升级绿点提醒。

## v0.8.0 - 2026-07-15

+ [新增] 画布节点插件系统:支持通过 URL 动态安装/启用/更新/卸载远程节点插件。
+ [新增] 插件开发 SDK,可用 TypeScript 开发画布节点插件。
+ [新增] 新增 Markdown、SVG、HTML、3D 全景、便利贴等示例插件。
+ [新增] 官方插件注册表:节点插件面板可从项目仓库读取官方插件列表并一键安装。
+ [新增] 在画布右上角工具栏新增「节点插件」入口。
+ [新增] 支持自定义生图/视频接口调用方式以适配不同中转站。

## v0.7.1 - 2026-07-15

+ [修复] 修复页面白屏报错的问题。

## v0.7.0 - 2026-07-14

+ [新增] Agent 对话消息改用 streamdown 流式渲染，提升Markdown 内容展示效果。
+ [新增] Agent 新增画布、工作台、提示词库和素材等站点级工具。
+ [新增] Agent 面板改为全站常驻右侧栏，开关时同步推动顶栏和页面内容。
+ [新增] Agent 新增 `site_navigate` 工具，支持页面跳转。
+ [新增] Agent 对话运行中支持一键停止，中断当前 Codex turn。
+ [新增] 画布节点支持统一维护名称字段，默认显示在节点上方，并可直接双击名称编辑。
+ [新增] 画布新增组节点，支持节点拖入/拆出分组、拖拽高亮吸附和移动组时带动子节点。
+ [新增] 画布空白区域支持双击打开节点选择菜单，并在点击位置创建节点。
+ [调整] Codex 会话改为站点级连续线程，跨页面和跨画布保持同一上下文。
+ [调整] 移除仅前端调用 OpenAI responses 接口，统一走 MCP + 本地 Codex 链路。
+ [调整] 画布节点顶部工具条改为点击选中节点后显示，避免鼠标经过节点时频繁弹出。
+ [优化] 本地 Agent 连接说明明确区分插件 / 手动 MCP 才会增加 Codex token 消耗。
+ [优化] 优化本地 Agent 连接说明，区分 Codex 插件启动和直接运行 Agent 两种方式。
+ [修复] 修复 Gemini 格式生图时因内置比例列表触发误报导致生成失败的问题。

## v0.6.0 - 2026-07-09

+ [新增] 新增Codex App插件支持。
+ [新增] 配置与用户偏好新增独立页面和 Codex 连接配置 Tab。
+ [新增] 新增GitHub Pages 前端静态站点发布 workflow。
+ [新增] 图片切图支持等分线直接拖拽调整，并可新增、删除和重置横向 / 纵向切图线。
+ [调整] Docker 运行镜像改为 nginx 静态托管。
+ [调整] 移除网站Agent模式，专注于连接Codex Agent操作画布
+ [修复] 修复生图工作台重试成功结果刷新后丢失的问题。
+ [修复] 修复 Gemini 调用格式生图未传递尺寸比例配置的问题。
+ [修复] 修复前端 TypeScript 构建报错。
+ [修复] 修复画布生成配置切换文本/视频/音频模式时模型仍显示为生图模型的问题。
+ [修复] 兼容中转站视频任务直接返回视频 URL 且没有 `/content` 接口的情况，并优化失败原因展示。

## v0.5.0 - 2026-07-05

+ [新增] 渠道兼容Gemini格式。
+ [调整] 前端从 Next.js 迁移到 Vite，项目改为静态前端构建。
+ [调整] 移除已 404 的 EvoLinkAI 提示词来源。

## v0.4.0 - 2026-06-16

+ [新增] 新增网页版Agent Loop模式。
+ [新增] 支持Vercel一键部署。
+ [调整] 移除后端，项目定位为个人画布工具。

## v0.3.0 - 2026-06-15

+ [新增] 新增canvas-agent通过codex操作画布。

## v0.2.5 - 2026-06-08

+ [新增] 新增图片切图功能。
+ [新增] 支持webdav同步数据。
+ [修复] 修复画布文字节点错误问题。

## v0.2.4 - 2026-06-04

+ [新增] 新增图片反推提示词功能。

## v0.2.3 - 2026-06-04

+ [新增] 新增图片蒙版局部修改功能。
+ [优化] 优化配置节点@图片功能。

## v0.2.2 - 2026-06-04

+ [新增] 新增图片放大工具。
+ [优化] 优化图片工具条，增加自定义功能。
+ [修复] 修复端口冲突问题、pg/mysql未初始化问题。

## v0.2.1 - 2026-06-03

+ [新增] 新增文档站点页面。
+ [优化] 优化画布连线交互。
+ [优化] 优化模型选择用户偏好。

## v0.2.0 - 2026-06-01

+ [新增] 支持通过火山方舟AgentPlan接入。
+ [新增] 视频生成支持声音、水印及图片/视频/音频参考输入。
+ [新增] 画布新增音频节点。
+ [优化] 图片/视频素材支持 `图片1`编号注入提示词。

## v0.1.1 - 2026-05-30

+ [新增] 支持New API跳转并自动填入Base URL和API Key配置。

## v0.1.0 - 2026-05-26

+ [优化] 优化我的画布、我的素材导出功能
+ [修复] 修复画布撤销，配置节点等bug问题

## v0.0.9 - 2026-05-26

+ [新增] 新增视频创作台页面。
+ [修复] 修复图片节点size参数传递问题。

## v0.0.8 - 2026-05-24

+ [新增] 新增用户账号与算力点体系，支持账号密码注册登录、Linux.do OAuth。
+ [新增] 管理后台公开配置支持设置模型算力点、支持计费查询。
+ [新增] 画布右上角展示用户算力点余额，生成按钮会展示本次预计消耗算力点。
+ [新增] 新增视频生成节点。

## v0.0.7 - 2026-05-23

+ [新增] 管理后台提示词管理支持多选批量删除。
+ [新增] 新增定义拉取GitHub提示词源功能。
+ [新增] 新增awesome-gpt-image2-prompts提示词来源。
+ [优化] 优化模型下拉选择样式、优化生图编辑设置

## v0.0.6 - 2026-05-22

+ [新增] 管理后台支持配置模型渠道，前端当前无需鉴权即可直接使用后端渠道能力。
+ [优化] 统一整理后端错误提示、AI 代理、图片节点生成与重试、参考图缺失处理等细节。
+ [优化] 后端模型代理路径调整为 OpenAI 风格。

## v0.0.5 - 2026-05-20

+ [新增] 右上角版本号支持点击查看版本更新弹窗，展示当前版本、最新版本和按时间线整理的更新日志。
+ [新增] 设置弹窗支持配置系统提示词，AI 生图、编辑图和文本请求会自动携带。

## v0.0.4 - 2026-05-20

+ [调整] Docker 运行入口改为 Next.js 对外提供页面，`/api/*` 由 Next.js 代理到内部 Go 服务。
+ [修复] 文本复制在局域网 IP 访问时可能失败的问题。

## v0.0.3 - 2026-05-19

+ [修复] 更新 nanoid 依赖并修改 ID 生成方式，防止其他ip无法使用crypto模块导致的ID生成失败问题。

## v0.0.2 - 2026-05-19

+ [新增] 增加生图工作台功能，支持文生图、图生图、查看历史记录，并增加移动端适配。
+ [修复] 画布生成尺寸控件支持选择更多常用比例，并可直接输入自定义比例。
+ [修复] 生成配置节点恢复拖拽操作，避免面板控件拦截整块节点拖动。
+ [文档] 增加 Render 部署说明。

## v0.0.1 - 2026-05-19

+ [新增] 首次开源版本，包含无限画布能力：多画布项目、节点拖拽缩放、连线、小地图、撤销重做、导入导出。
+ [新增] AI 创作能力：支持 OpenAI 兼容接口的文生图、图生图、参考图编辑和文本问答。
+ [新增] 画布助手能力：支持围绕选中节点和上游节点对话、生图，并把结果插回画布。
+ [新增] 提示词库能力：抓取多个 GitHub 开源项目，按案例整理数百个图片提示词。
