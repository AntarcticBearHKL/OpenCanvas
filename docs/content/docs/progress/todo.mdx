---
title: TODO
description: Planned follow-up work
---

# TODO

## VST3 音源（本地宿主）

- VST3 效果器：音轨内联效果链路（宿主 `role: "effect"` + `POST /audio-in` + 轨道上的 `vst3Effect` 槽位）已打通，但还没有选择效果器的 UI，也没支持发送轨 / 总线上插入，离线导出暂不包含效果处理（没有效果 stem）。
- 32 位与 ARM64 插件支持；原生低延迟输出（WASAPI 独占 / ASIO）——后者是唯一能把 MIDI → 声音压到 10ms 以内的路径。
